---
phase: 06-edge-case-sweeps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_edge_cases.py
  - .planning/PROJECT.md
  - .planning/phases/06-edge-case-sweeps/06-pytest-output.txt
  # EDG-04's T=0/B=0 guard lands in GRULayer.forward — a single guard there
  # covers all 7 GRULayer-routed paths (the dispatch routing is verified: see
  # Task 1). gru_layer.py is the one src/ file Phase 6 expects to touch for the guard.
  - src/gru_qat/gru_layer.py
  # CONDITIONAL — added only if an edge sweep (Task 3) surfaces a real BLOCK-size
  # kernel bug (D-04 fix-in-phase). The kernel files are listed because Task 3's
  # bug disposition may require a kernel fix; they are NOT touched by the EDG-04 guard.
  - src/gru_qat/triton_kernels/scan.py
  - src/gru_qat/triton_kernels/scan_diagonal.py
  - src/gru_qat/triton_kernels/scan_monarch.py
  - src/gru_qat/triton_kernels/scan_butterfly.py
  - src/gru_qat/structure.py
autonomous: true
requirements: [EDG-01, EDG-02, EDG-03, EDG-04]

must_haves:
  truths:
    - "tests/test_edge_cases.py covers T=1 fwd+bwd for every available path, parity vs reference at the path's normal tolerance tier (EDG-01, ROADMAP SC#1)"
    - "Same file covers B=1 and H in {1,2} for every path, explicitly the BLOCK-size-assumption failure modes from CONCERNS.md (EDG-02, ROADMAP SC#2)"
    - "T in {512,1024} long-sequence tests marked @pytest.mark.slow assert no accumulated drift beyond tier-A tolerance (EDG-03, ROADMAP SC#3)"
    - "T=0 and B=0 raise a clear ValueError naming the offending dimension on every path; no NaN, no kernel hang; policy logged in PROJECT.md (EDG-04, ROADMAP SC#4)"
    - "Any mismatch surfaced becomes a failing test then bd issue then in-phase fix; no @pytest.mark.xfail anywhere (ROADMAP SC#5, D-04/D-05)"
  decisions:
    - "D-01: every path raises ValueError on T=0/B=0, message names the offending dimension (T or B) — verified by Task 1 EDG-04 tests."
    - "D-02: the T=0/B=0 ValueError disposition is logged in PROJECT.md — Task 1 writes the policy paragraph."
    - "D-03: a path currently hanging/NaN-ing at T=0/B=0 is a finding handled under D-04/D-05 (fix-in-phase); guard placement is GRULayer.forward (policy locked, placement delegated)."
    - "D-06: a deep BLOCK-size kernel fix enlarging the phase is the accepted tradeoff — Task 3 may legitimately touch kernel code."
    - "D-07: all 7 paths x all edge dimensions, realistic inputs only — no adversarial cross-product (Phase 4 owns adversarial numerics)."
    - "D-08: uniform coverage — circulant and LDR per-step paths get the same edge dimensions as the Triton paths, not a reduced subset (ALL_PATHS has 7 entries)."
    - "D-09: long-T tests are @pytest.mark.slow; tolerances reused verbatim from PROJECT.md (reference <1e-4, diagonal <1e-5, dense/monarch/butterfly <5e-4) — no new bounds."
    - "D-10: all edge sweeps live in a single new tests/test_edge_cases.py, parametrized over path x shape."
    - "D-11: the reference-vs-nn.GRU translation helper is imported from the D-51-locked test_layer_parity.py — import-only, the locked file is never edited."
  artifacts:
    - path: "tests/test_edge_cases.py"
      provides: "Path x edge-shape parametrized sweep for all 7 GRU paths"
      contains: "def test_t1_"
      min_lines: 250
    - path: ".planning/PROJECT.md"
      provides: "Logged T=0/B=0 ValueError disposition (Phase 6, EDG-04 SC#4)"
      contains: "T=0"
    - path: ".planning/phases/06-edge-case-sweeps/06-pytest-output.txt"
      provides: "Timestamped pytest-output artifact for the Phase 6 sweep"
  key_links:
    - from: "tests/test_edge_cases.py"
      to: "tests/test_layer_parity.py"
      via: "importlib.import_module of _translate_nn_gru_to_cell (sibling import, D-11 import-only)"
      pattern: "import_module\\(.test_layer_parity"
    - from: "tests/test_edge_cases.py (T=0/B=0 tests)"
      to: "GRULayer.forward"
      via: "pytest.raises(ValueError, match=...) against the guard"
      pattern: "pytest\\.raises\\(ValueError"
---

<objective>
Pin every GRU code path at boundary shapes — proving that T=1, B=1, H in {1,2}, long
T in {512,1024}, and degenerate T=0/B=0 inputs either produce correct output (at the
path's normal tolerance tier) or fail with a clear, tested `ValueError`.

Purpose: This is the edge-shape ring of the native-PyTorch parity audit. Phase 4
already swept adversarial numerics; Phase 6 owns *shape* robustness. CONCERNS.md
predicts BLOCK-size-assumption failures at tiny shapes (butterfly `B % BLOCK_B`,
monarch non-pow2 `BLKSZ`, persistent-grid deadlock) — this sweep targets exactly
those failure modes. When the sweep surfaces a real bug, it is fixed in-phase.

Output:
- `tests/test_edge_cases.py` — a single new file, parametrized over 7 paths x edge shapes.
- A T=0/B=0 `ValueError` guard in `GRULayer.forward` (covers all 7 GRULayer-routed paths).
- The T=0/B=0 policy logged in `PROJECT.md`.
- A timestamped pytest-output artifact at `06-pytest-output.txt`.

Single plan by design: `tests/test_edge_cases.py` is one new file. Per CONTEXT.md
D-10/D-11, all edge sweeps live in this one file, and a single plan eliminates the
F-04-05-D / `gru-triton-u00` parallel-write race. Tasks run sequentially in one wave.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-edge-case-sweeps/06-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase — do NOT re-explore. -->

The 7 paths to parametrize (CONTEXT.md <domain>):
  1. reference   — GRULayer(use_triton=False), dense, CPU-OK
  2. dense_triton    — GRULayer with dense hidden, use_triton=True, CUDA
  3. diagonal_triton — structure_hidden=StructureConfig(kind="diagonal"), use_triton=True, CUDA
  4. monarch_triton  — structure_hidden=StructureConfig(kind="monarch"),  use_triton=True, CUDA
  5. butterfly_triton— structure_hidden=StructureConfig(kind="butterfly"),use_triton=True, CUDA
  6. circulant   — structure_hidden=StructureConfig(kind="circulant"), per-step PyTorch, CPU-OK
  7. ldr         — structure_hidden=StructureConfig(kind="ldr"),       per-step PyTorch, CPU-OK
     (circulant/ldr are NOT in the fast-dispatch set — gru_layer.py:100-104 — so they
      always run the per-step path regardless of use_triton.)

GRULayer construction (src/gru_qat/gru_layer.py:49):
  GRULayer(input_size, hidden_size, recipe, *, batch_first=False, gate_layout="split",
           pre_batch_input=False, structure_input=None, structure_hidden=None,
           use_triton="auto")
  - Triton fast-path eligibility (gru_layer.py:100-104): structure_input is None AND
    structure_hidden.kind in {diagonal,monarch,butterfly} AND gate_layout="fused".
  - GRULayer.forward(x, h0=None) -> (out, h_T). x is [seq, batch, input_size]
    (or batch_first). seq_len/batch_size unpacked at gru_layer.py:158.
  - Dispatch routing (verified for D-01): the fast path (line 167) and the two
    per-step branches (structured at line ~176, dense at line ~189) ALL run
    downstream of the `seq_len, batch_size, _ = x.shape` unpack at line 158. So a
    SINGLE guard placed right after line 158 covers all 7 GRULayer-routed paths —
    including circulant/ldr, which fall through to the per-step loop.

Reference-vs-nn.GRU translation helper (import-only — D-11 allows importing from
the D-51 LOCKED test_layer_parity.py; editing it is forbidden):
  tests/test_layer_parity.py:99  _translate_nn_gru_to_cell(gru: nn.GRU) -> GRULayer
    Builds a dense fp32-Identity GRULayer whose weights are chunk-split copies of
    an nn.GRU's concatenated parameters. This is the helper Task 2/3/4 use to compare
    the reference path against torch.nn.GRU.
  Import idiom (proven in test_calibration.py:32-67): tests/ has NO __init__.py, so
  pytest's prepend import mode makes sibling test modules importable —
  `importlib.import_module("test_layer_parity")` then access `_translate_nn_gru_to_cell`.

Phase 4 strict-file helper (import-only — D-51 LOCKED, may NOT edit these files):
  test_triton_monarch_strict.py:659 _skip_if_monarch_bwd_hw_limit(T, B, H, nblocks)
    The one genuinely shared helper Phase 6 imports from a strict file. Phase 6
    builds its own minimal local fp32-Identity layer factories (the strict files'
    factories are int8-quant; Phase 6 needs fp32-Identity for shape-parity) — see
    patterns in test_triton_diagonal.py:35 / test_triton_monarch.py:35 /
    test_butterfly_dispatch.py:36.

Existing ValueError shape-validation convention to MIRROR:
  src/gru_qat/gru_cell.py:107 — raise ValueError("fused gate layout requires
    recipe.weight.axis=0; got axis={...}. ...")
  src/gru_qat/structure.py:79  — raise ValueError("diagonal requires square
    (in == out); got in={...}, out={...}")

Tolerance tiers (PROJECT.md Constraints — REUSE verbatim, D-09; do NOT invent):
  - reference layer vs nn.GRU:          < 1e-4 absolute
  - Triton vs reference, non-tl.dot (diagonal): < 1e-5 absolute
  - Triton vs reference, tl.dot (dense, monarch, butterfly): < 5e-4 absolute
  - circulant/ldr per-step vs reference: these run the PyTorch path; for shape
    parity assert finite + correct shape (smoke), and where a same-recipe reference
    exists assert < 1e-5 (algebraic equality, per TESTING.md "Bit-identical" regime).

Test gating (TESTING.md "How CUDA/Triton Availability Is Gated"):
  - Triton-path tests: per-test @cuda_only + module-or-body pytest.importorskip("triton").
  - monarch/butterfly/ldr also need torch_structured -> pytest.importorskip("torch_structured").
  - reference + circulant tests run CPU-side, NO @cuda_only.
  - Use the test_calibration.py pattern: function-level @cuda_only decorator,
    NOT a module-level pytest.importorskip (keeps CPU-side tests collectable).
  - Numeric tests set torch.manual_seed(0) + torch.set_float32_matmul_precision("high").

CONCERNS.md BLOCK-size failure modes the B=1/H-small sweep MUST target:
  - butterfly: B % BLOCK_B != 0 partial-tile OOB (d8218d4 shipped WITHOUT a regression
    test). B=1 is the extreme; also sweep B in {1,3,5,7,9,17,33} for butterfly.
  - monarch: non-pow2 BLKSZ pad-to-pow2 + mask fragility at small H.
  - dense/monarch persistent grid: cdiv(B,block_b)*cdiv(H,block_oh) <= sm_count
    deadlock guard (RuntimeError). NOTE: Task 3's shapes are all small enough
    (H<=8, B<=33; default block_b=8, block_oh=128 -> grid product is 1 for every
    Task-3 shape, far under sm_count ~24 on RTX 2000 Ada) that NONE trip this guard.
    The guard is documented here for context only; Task 3 does not test it.
  - HW-limit skip (_skip_if_monarch_bwd_hw_limit) is LEGITIMATE for shapes the
    RTX 2000 Ada kernels genuinely cannot launch. A HW-limit skip is DISTINCT
    from a BLOCK-assumption bug — a real bug must be FIXED (D-04), not skipped.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: T=0/B=0 ValueError guard in GRULayer.forward + EDG-04 tests + PROJECT.md policy log</name>
  <files>tests/test_edge_cases.py, .planning/PROJECT.md, src/gru_qat/gru_layer.py</files>
  <read_first>
    - tests/test_calibration.py — Phase 5 single-file precedent: cross-file import idiom, function-level @cuda_only, synthetic-loader helpers.
    - .planning/codebase/CONCERNS.md — the "Asserts used for shape validation in hot kernel wrappers" note (Python -O strips asserts) — the guard must be `if ... raise ValueError`, NOT assert.
    - src/gru_qat/gru_layer.py — GRULayer.forward, seq_len/batch_size unpack at line 158; the natural and complete home for the guard (all 7 paths route through forward()).
    - src/gru_qat/gru_cell.py — line 107 ValueError convention to mirror (message names the offending field).
    - .planning/PROJECT.md — Constraints + Key Decisions sections (where the policy is logged).
  </read_first>
  <action>
    Create the new file `tests/test_edge_cases.py`. Module docstring states: Phase 6
    edge-case shape sweep, all 7 paths, realistic inputs only (D-07), single race-free
    file (D-10/D-11). Add the function-level `cuda_only = pytest.mark.skipif(not
    torch.cuda.is_available(), ...)` decorator (mirror test_calibration.py:27-29) — do
    NOT use a module-level importorskip. Define minimal local factories
    `_make_layer(path: str, in_size, hid)` returning a correctly-configured fp32-Identity
    `GRULayer` per the 7-path table in <interfaces> (fused gate_layout for the Triton-
    eligible kinds; use_triton set per path). Define the path constant list
    `ALL_PATHS = ["reference","dense_triton","diagonal_triton","monarch_triton",
    "butterfly_triton","circulant","ldr"]`.

    EDG-04 GUARD POLICY (D-01/D-02/D-03 — LOCKED): every path must raise `ValueError`
    on T=0 or B=0, message naming the offending dimension. First, run each path at
    `(T=0, B=2, H=4)` and `(T=4, B=0, H=4)` and observe behavior. For any path that
    does NOT already raise a clear ValueError naming `T`/`B`, add the guard in ONE
    place: `GRULayer.forward`, right after the `seq_len, batch_size, _ = x.shape`
    unpack (gru_layer.py:158). Form: `if seq_len == 0: raise ValueError("seq length T
    must be > 0; got T=0")` and the matching `batch_size == 0` -> `B`. The message
    MUST contain the substring `T` (for T=0) or `B` (for B=0), mirroring the
    gru_cell.py:107 convention. Use `if ... raise`, never `assert` (Python -O strips
    asserts per CONCERNS.md).

    D-01's scope is `GRULayer.forward`-routed calls — the 7 paths are the 7 `GRULayer`
    configurations in <interfaces>. The dispatch routing is verified: the fast path,
    the structured per-step branch, and the dense per-step branch ALL run downstream
    of the line-158 unpack, and circulant/ldr are not fast-dispatch-eligible
    (gru_layer.py:99-104) so they fall through forward()'s per-step loop. A SINGLE
    guard after line 158 therefore covers all 7 paths — no per-path or per-wrapper
    guard is needed. The four `gru_scan*` Triton wrappers are an internal API and are
    OUT of EDG-04 scope; their existing shape `assert`s are a separate hardening item.
    File a bd issue (`bd create --type=bug`) noting that `assert`-based shape
    validation in the `scan*.py` wrappers gets stripped under `python -O`, but DO NOT
    fix it in Phase 6 — it is not an EDG-04 deliverable.

    Write the EDG-04 test: `test_t0_b0_raises_valueerror` parametrized over
    `path in ALL_PATHS` and over `(bad_dim, shape)` in
    `[("T", (0, 2, 4)), ("B", (4, 0, 4))]`. Each test builds the path's layer, calls
    `layer.forward(torch.randn(*shape))`, and asserts
    `pytest.raises(ValueError, match=bad_dim)`. Triton-path cases get `@cuda_only` +
    `pytest.importorskip("triton")`; monarch/butterfly/ldr also importorskip
    "torch_structured". Assert NO NaN and NO hang — the ValueError must raise promptly
    (the test passing IS the no-hang proof; a hung kernel never returns to assert).

    Log the policy in `.planning/PROJECT.md`. Add a row to the Key Decisions table:
    `| Phase 6 D-01 (T=0/B=0 disposition) | An empty sequence/batch is almost always
    a caller bug; Triton kernels cannot launch a 0-size grid. Uniform fail-loud matches
    kernel reality. | All 7 paths raise ValueError naming the offending dimension (T or
    B) on T=0 / B=0 — no empty-output passthrough. Resolves ROADMAP Phase 6 SC#4 to the
    ValueError branch. |`. This satisfies ROADMAP SC#4's "logged in PROJECT.md".

    If observing a path surfaces a real bug other than a missing guard (e.g. a kernel
    hangs or NaNs even with the guard removed at a non-degenerate shape), follow
    D-04/D-05: commit the failing test FIRST (Commit A), `bd create` an issue, fix it
    (Commit B), confirm green. Do NOT use `@pytest.mark.xfail`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_edge_cases.py -k "t0_b0" -q</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `uv run pytest tests/test_edge_cases.py -k "t0_b0" -q` exits 0 on the host (Triton cases skip cleanly on CPU-only hosts via @cuda_only).
    - Behavior: passing `(T=0, B=2, H=4)` to the reference path raises `ValueError` whose message contains the substring `T`.
    - Behavior: passing `(T=4, B=0, H=4)` to the reference path raises `ValueError` whose message contains the substring `B`.
    - Source: `tests/test_edge_cases.py` contains `def test_t0_b0_raises_valueerror(`.
    - Source: the only `src/gru_qat/**` file modified by this task is `gru_layer.py` — `git diff --name-only` shows no `scan*.py` or `structure.py` change for the guard.
    - Source: `.planning/PROJECT.md` contains the substring `T=0` in a Key Decisions row referencing Phase 6.
    - No `@pytest.mark.xfail` token anywhere in `tests/test_edge_cases.py` or modified `src/gru_qat/**` files.
  </acceptance_criteria>
  <done>All 7 paths raise a clear ValueError naming the offending dimension on T=0/B=0 via a single `GRULayer.forward` guard; the EDG-04 test passes; the policy is logged in PROJECT.md; a bd issue is filed (not fixed) for the `scan*.py` wrapper `assert` hardening item. One atomic commit (`test(06-01-01): T=0/B=0 ValueError guard in GRULayer.forward + EDG-04 tests + PROJECT.md policy`); if a bug surfaced, a Commit A / Commit B pair with a bd issue.</done>
</task>

<task type="auto">
  <name>Task 2: T=1 single-timestep fwd+bwd sweep (EDG-01)</name>
  <files>tests/test_edge_cases.py</files>
  <read_first>
    - tests/test_edge_cases.py — the file being extended (factories + ALL_PATHS from Task 1).
    - tests/test_layer_parity.py — D-51 LOCKED, import-only: `_translate_nn_gru_to_cell` at line 99 is the nn.GRU->cell weight-copy helper Task 2 imports.
    - tests/test_calibration.py:32-67 — the proven sibling-import idiom (`importlib.import_module`) for pulling a helper out of a sibling test module.
    - tests/test_triton_diagonal.py, tests/test_triton_monarch.py, tests/test_butterfly_dispatch.py — Phase 2-era base files: patterns for building per-path layers, `_build_gi_from_cell`, relative-error reporting idiom with the 1e-6 floor.
    - .planning/codebase/TESTING.md — "Parity Tests vs Numerical-Tolerance Tests" tier definitions; "Common Patterns to Match When Adding Tests".
  </read_first>
  <action>
    Extend `tests/test_edge_cases.py`. Add `test_t1_forward_parity` and
    `test_t1_backward_parity`, each parametrized over `path in ALL_PATHS` at the single
    edge shape `(T=1, B=4, H=8)` (T=1 is the boundary; B and H non-degenerate so this
    test isolates the single-timestep concern — B=1/H-small is Task 3).

    Reference-vs-`nn.GRU` comparison helper — ONE approach, decided at plan time:
    import `_translate_nn_gru_to_cell` from `tests/test_layer_parity.py` via the
    sibling-import idiom. `tests/` has NO `__init__.py` (confirmed), so pytest's
    prepend import mode makes the sibling module importable. At module scope in
    `test_edge_cases.py`, add:
    `_layer_parity = importlib.import_module("test_layer_parity")` and reference
    `_layer_parity._translate_nn_gru_to_cell`. This is import-only — `test_layer_parity.py`
    is D-51 LOCKED and is NOT edited. Do NOT add a runtime "if not importable, fall
    back to a local copy" branch — the import is committed-to; if it ever fails that
    is a hard error, not a silent degradation.

    `test_t1_forward_parity`: build the path's layer and the reference layer
    (`use_triton=False`, dense) with the SAME weights (copy parameters so both see
    identical math). Run forward on the same `torch.randn(1, 4, 8)` input
    (manual_seed(0), set_float32_matmul_precision("high")). Assert `(out, h_T)` parity:
      - reference path: build a fresh `nn.GRU(input_size, hidden_size,
        batch_first=False)`, derive the matched reference `GRULayer` via
        `_layer_parity._translate_nn_gru_to_cell(gru)`, run both, and compare at
        < 1e-4 absolute.
      - dense_triton / monarch_triton / butterfly_triton: vs the reference path at
        < 5e-4 absolute (tl.dot tier).
      - diagonal_triton: vs the reference path at < 1e-5 absolute (non-tl.dot tier).
      - circulant / ldr: per-step PyTorch — assert finite + shape `(1,4,8)` and, where
        a same-recipe reference run exists (the path run twice is deterministic),
        assert < 1e-5; otherwise smoke (finite + shape).
    Use the relative-error reporting idiom from TESTING.md (1e-6 floor) in the assert
    message so a regression localizes.

    `test_t1_backward_parity`: same grid; build a scalar loss `out.float().pow(2).sum()`,
    call `.backward()`, and assert gradients exist and are finite on every learnable
    parameter, AND that the gradient on `x` (and on each weight) matches the reference
    path's autograd within the same per-path tolerance tier as forward. Use
    `_skip_if_monarch_bwd_hw_limit(T=1, B=4, H=8, nblocks=...)` for the monarch backward
    case (imported from test_triton_monarch_strict.py) — a HW-limit skip is legitimate;
    a BLOCK-assumption bug is not (D-04: if monarch bwd fails at T=1 for a NON-HW reason,
    fix it in-phase per Task-level D-04/D-05 discipline).

    Triton-path cases: `@cuda_only` + `pytest.importorskip("triton")`;
    monarch/butterfly/ldr also `importorskip("torch_structured")`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_edge_cases.py -k "t1" -q</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `uv run pytest tests/test_edge_cases.py -k "t1" -q` exits 0 on the host (Triton cases skip cleanly on CPU-only hosts).
    - Source: `tests/test_edge_cases.py` contains `def test_t1_forward_parity(` and `def test_t1_backward_parity(`.
    - Source: `tests/test_edge_cases.py` imports `_translate_nn_gru_to_cell` via `importlib.import_module("test_layer_parity")` — no local re-implementation of the nn.GRU weight copy, no runtime import-fallback branch.
    - Source: the parametrize grid for both tests names all 7 entries of `ALL_PATHS` and the shape tuple `(1, 4, 8)`.
    - Behavior: the reference-path T=1 forward case asserts parity vs `torch.nn.GRU` at `< 1e-4`; the diagonal_triton case asserts `< 1e-5`; the dense/monarch/butterfly cases assert `< 5e-4` (tolerances reused verbatim from PROJECT.md, D-09 — no new bounds).
    - Source: D-51 locked file `tests/test_layer_parity.py` is unchanged (`git diff --name-only` shows it not modified).
  </acceptance_criteria>
  <done>T=1 fwd+bwd parity passes for all 7 paths at the path's normal tolerance tier; the nn.GRU comparison uses the imported `_translate_nn_gru_to_cell` helper. One atomic commit (`test(06-01-02): T=1 single-timestep fwd+bwd sweep (EDG-01)`); if a bug surfaced, a Commit A / Commit B pair with a bd issue.</done>
</task>

<task type="auto">
  <name>Task 3: B=1 + H in {1,2} BLOCK-size sweep (EDG-02)</name>
  <files>tests/test_edge_cases.py</files>
  <read_first>
    - tests/test_edge_cases.py — the file being extended.
    - .planning/codebase/CONCERNS.md — THE source for the BLOCK-size failure modes this task targets: butterfly `B % BLOCK_B` partial-tile OOB (~lines 57-62, 124-128, 225-229), monarch non-pow2 `BLKSZ` pad-to-pow2 (~130-134), persistent-grid `cdiv<=sm_count` deadlock (~110-114, 155-158).
    - tests/test_triton_monarch_strict.py — `_skip_if_monarch_bwd_hw_limit` (line 659): distinguish a legitimate HW-limit skip from a BLOCK-assumption bug.
    - .planning/codebase/TESTING.md — "Per-batch error inspection" idiom (`rel_per_b`) for localizing grid/parallelism bugs to a specific `pid_b`.
  </read_first>
  <action>
    Extend `tests/test_edge_cases.py`. This is the most bug-likely task per CONCERNS.md
    — the B=1 / small-H corner is exactly where Triton BLOCK assumptions break.

    Add `test_b1_small_h_parity` parametrized over `path in ALL_PATHS` and over the
    explicit shape grid `[(T, B, H)]` =
    `[(8, 1, 1), (8, 1, 2), (8, 1, 8), (8, 4, 1), (8, 4, 2)]`
    (B=1 single-batch x H in {1,2}, plus B=1/H=8 and B=4/H in {1,2} so the B and H
    degeneracies are isolated and crossed). For each (path, shape): build the path layer
    + a same-weights reference layer, run forward on `torch.randn(T, B, H_in)` where
    `H_in` is the input size (use a fixed `in_size=4`), and assert `(out, h_T)` parity
    at the path's tolerance tier (reference < 1e-4 vs nn.GRU; diagonal_triton < 1e-5;
    dense/monarch/butterfly < 5e-4; circulant/ldr finite+shape, < 1e-5 where a
    same-recipe reference exists). Use the `rel_per_b` per-batch error idiom in the
    assert message so a grid bug localizes to a `pid_b`.

    Add `test_butterfly_partial_batch_tile` (`@cuda_only` + `importorskip("triton")` +
    `importorskip("torch_structured")`) parametrized over `B in [1, 3, 5, 7, 9, 17, 33]`
    at `(T=16, H=512)` — the CONCERNS.md-suggested butterfly sweep covering the
    `B % BLOCK_B != 0` partial-last-tile corner (`d8218d4` shipped without a regression
    test). Build a butterfly layer + reference, forward, assert parity at < 5e-4 with
    the per-batch idiom.

    Note on the persistent-grid `cdiv<=sm_count` RuntimeError deadlock guard: every
    Task-3 shape is small (H<=8 in `test_b1_small_h_parity`; butterfly B<=33 / H=512
    in `test_butterfly_partial_batch_tile`). With the default `block_b=8` /
    `block_oh=128`, `cdiv(B,block_b)*cdiv(H,block_oh)` is 1 for every Task-3 shape —
    far below `sm_count` (~24 on RTX 2000 Ada). NO Task-3 shape trips the SM-count
    cap, so this task does NOT add a speculative `pytest.raises(RuntimeError)` —
    every Task-3 shape is expected to run the persistent path successfully.

    Crucial D-04 discipline for this task: CONCERNS.md PREDICTS bugs here. If a (path,
    shape) case fails for a BLOCK-assumption reason (OOB, wrong-answer at the partial
    tile, non-pow2 BLKSZ mask gap), that is a REAL BUG — fix it in-phase:
    commit the failing test FIRST (Commit A) -> `bd create` an issue capturing root
    cause + the failing shape -> fix the kernel in `src/gru_qat/triton_kernels/scan_*.py`
    (Commit B) -> confirm the test passes. NO `@pytest.mark.xfail`. A deep BLOCK-size
    kernel fix enlarging this task is the accepted tradeoff (D-06). Distinguish: if a
    monarch shape genuinely cannot launch on RTX 2000 Ada hardware,
    `_skip_if_monarch_bwd_hw_limit` is a legitimate skip — but a skip is NOT a
    disposition for a wrong-answer bug.

    CONTEXT-BUDGET HANDOFF (WARNING 1): if a surfaced BLOCK-size bug requires a deep
    kernel fix, the executor MUST pause and hand off rather than exhausting context
    trying to finish all 5 tasks in one execution. Commit A (the failing test) and the
    `bd create` issue MUST land before the handoff so the finding is recoverable; the
    executor records the handoff state in the plan summary. A deep kernel fix is a
    legitimate stopping point — Phase 6 is an audit phase (D-06).
  </action>
  <verify>
    <automated>uv run pytest tests/test_edge_cases.py -k "b1 or partial_batch" -q</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `uv run pytest tests/test_edge_cases.py -k "b1 or partial_batch" -q` exits 0 on the host (Triton cases skip cleanly on CPU-only hosts).
    - Source: `tests/test_edge_cases.py` contains `def test_b1_small_h_parity(` and `def test_butterfly_partial_batch_tile(`.
    - Source: the `test_b1_small_h_parity` parametrize grid contains the literal tuples `(8, 1, 1)` and `(8, 1, 2)`; the `test_butterfly_partial_batch_tile` grid contains the literal `B` values `1, 3, 5, 7, 9, 17, 33`.
    - Source: no `pytest.raises(RuntimeError)` instruction in Task 3's tests — all Task-3 shapes are below the SM-count cap.
    - Behavior: any BLOCK-size failure surfaced is fixed in-phase (Commit A failing test precedes Commit B fix, verifiable via `git log`) with a bd issue — never silenced with `@pytest.mark.xfail`.
  </acceptance_criteria>
  <done>B=1 and H in {1,2} produce correct output for all 7 paths; the butterfly partial-batch-tile sweep passes at B in {1,3,5,7,9,17,33}; any BLOCK-assumption bug surfaced is fixed in-phase. One atomic commit (`test(06-01-03): B=1 + small-H BLOCK-size sweep (EDG-02)`); per surfaced bug a Commit A / bd issue / Commit B sub-sequence. If a deep kernel fix is required, the executor pauses and hands off AFTER Commit A + the bd issue land — do not exhaust context completing all 5 tasks in one run.</done>
</task>

<task type="auto">
  <name>Task 4: Long-T slow-tier sweep (EDG-03)</name>
  <files>tests/test_edge_cases.py</files>
  <read_first>
    - tests/test_edge_cases.py — the file being extended.
    - .planning/codebase/TESTING.md — `slow` marker definition and usage; tiered-tolerance regimes.
    - .planning/PROJECT.md — Constraints tolerance tiers + the F-02-02-A note (diagonal long-T `dbh` drift loosened to < 2e-5 slow-tier — reuse, do not re-derive).
  </read_first>
  <action>
    Extend `tests/test_edge_cases.py`. Add `test_long_t_drift` decorated with
    `@pytest.mark.slow`, parametrized over `path in ALL_PATHS` and over
    `T in [512, 1024]` at `(B=4, H=8)`. For each (path, T): build the path layer + a
    same-weights reference layer, run forward on `torch.randn(T, 4, 4)`
    (in_size=4, manual_seed(0), set_float32_matmul_precision("high")), and assert no
    accumulated drift beyond the tier-A tolerance:
      - reference path: vs `torch.nn.GRU` (via the imported `_translate_nn_gru_to_cell`)
        at < 1e-4 absolute (layer tier-A).
      - diagonal_triton: vs reference at < 1e-5 absolute (non-tl.dot tier).
      - dense/monarch/butterfly_triton: vs reference at < 5e-4 absolute (tl.dot tier).
      - circulant/ldr: vs a same-recipe deterministic reference run at < 1e-5, else
        finite + shape.
    Tolerances reused verbatim from PROJECT.md (D-09) — do NOT invent new long-T
    bounds. (Note: PROJECT.md records the diagonal long-T `dbh` slow-tier loosening to
    < 2e-5 from F-02-02-A; this task is forward-output drift, which stays at the < 1e-5
    diagonal tier — only mention the < 2e-5 bound if a backward long-T case is added.)

    Use the `rel`-with-1e-6-floor reporting idiom in the assert message. The
    `@pytest.mark.slow` mark MUST be present so `pytest -m "not slow"` skips these
    (ROADMAP SC#3). Triton-path cases additionally `@cuda_only` + `importorskip`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_edge_cases.py -k "long_t" -m slow -q</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `uv run pytest tests/test_edge_cases.py -k "long_t" -m slow -q` exits 0 on the host (Triton cases skip cleanly on CPU-only hosts).
    - Behavior: `uv run pytest tests/test_edge_cases.py -k "long_t" -m "not slow" -q` collects 0 tests (the slow mark deselects them).
    - Source: `tests/test_edge_cases.py` contains `def test_long_t_drift(` decorated with `@pytest.mark.slow`.
    - Source: the parametrize grid contains the literal `T` values `512` and `1024`.
  </acceptance_criteria>
  <done>T in {512,1024} long-sequence parity passes for all 7 paths with no drift beyond tier-A tolerance; tests are `@pytest.mark.slow`. One atomic commit (`test(06-01-04): long-T slow-tier drift sweep (EDG-03)`); if a bug surfaced, a Commit A / Commit B pair with a bd issue.</done>
</task>

<task type="auto">
  <name>Task 5: Full-suite run + timestamped pytest-output artifact</name>
  <files>tests/test_edge_cases.py, .planning/phases/06-edge-case-sweeps/06-pytest-output.txt</files>
  <read_first>
    - tests/test_edge_cases.py — the full file (review for completeness: all 4 test functions present, all 7 paths parametrized, gating correct).
    - .planning/phases/05-calibration-freeze-lifecycle/ — Phase 5 retrospective recommendation #1 (timestamped pytest-output artifact precedent).
  </read_first>
  <action>
    Final sweep + artifact capture (Phase 5 retrospective recommendation #1).

    First run lint/type gates on the `gru_layer.py` guard change from Task 1:
    `uv run ruff check src tests` and `uv run mypy` — both must be green (the project
    session-completion protocol requires quality gates; new test helpers in `tests/`
    are not mypy-strict, but the guard added to `src/gru_qat/gru_layer.py` is).

    Then run the full edge-case suite including slow tests and capture the output to a
    timestamped artifact:
    `uv run pytest tests/test_edge_cases.py -v 2>&1 | tee
    .planning/phases/06-edge-case-sweeps/06-pytest-output.txt` followed by
    `uv run pytest tests/test_edge_cases.py -m slow -v 2>&1 | tee -a
    .planning/phases/06-edge-case-sweeps/06-pytest-output.txt`. Prepend a header line
    to the artifact recording the UTC timestamp and the host CUDA availability
    (`torch.cuda.is_available()` + device name if present) so a reader knows whether
    the Triton paths actually executed or were skipped.

    Confirm the suite is green: every path x every edge dimension either passes parity
    or raises the expected ValueError. If ANY test is still red, it is an in-phase bug
    (D-04) — Commit A failing test already exists, `bd create` the issue, fix it
    (Commit B), re-run. The artifact must reflect a final green run. NO `@pytest.mark.xfail`.

    Confirm the existing wider suite still passes — run `uv run pytest -q -m "not slow"`
    to verify the T=0/B=0 guard added in Task 1 did not regress any existing test
    (the guard is a new ValueError on a degenerate shape; no existing test should pass
    T=0/B=0, but verify).
  </action>
  <verify>
    <automated>uv run pytest tests/test_edge_cases.py -q && test -s .planning/phases/06-edge-case-sweeps/06-pytest-output.txt</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `uv run pytest tests/test_edge_cases.py -q` exits 0 (full edge-case file green; Triton cases skip cleanly on CPU-only hosts).
    - Behavior: `uv run pytest -q -m "not slow"` exits 0 (no existing test regressed by the Task 1 guard).
    - Behavior: `uv run ruff check src tests` exits 0 and `uv run mypy` exits 0.
    - Source: `.planning/phases/06-edge-case-sweeps/06-pytest-output.txt` exists, is non-empty, and contains a UTC-timestamp header line plus the pytest summary line.
    - No `@pytest.mark.xfail` token in `tests/test_edge_cases.py`.
  </acceptance_criteria>
  <done>The full Phase 6 edge-case suite is green; ruff + mypy are green; a timestamped pytest-output artifact is captured. One atomic commit (`test(06-01-05): full edge-case suite run + timestamped pytest-output artifact`).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 6 adds test coverage + a small input-validation guard. No new attack surface —
the T=0/B=0 `ValueError` guard REDUCES risk by rejecting malformed input early
(fail-loud instead of a silent NaN or a hung kernel). `gru-triton` is a library, not
a service: no network surface, no untrusted-input deserialization (CONCERNS.md
§Security). The only behavior change is converting degenerate-shape inputs from
undefined behavior into a clear, tested `ValueError`.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `GRULayer.forward` with T=0/B=0 input | mitigate | `if seq_len == 0 / batch_size == 0: raise ValueError` naming the dim — a single guard after the line-158 unpack replaces undefined kernel behavior (0-size grid launch, OOB) with a deterministic, tested error for all 7 GRULayer-routed paths. |
| T-06-02 | Denial of Service | Triton persistent kernel at degenerate shapes | accept | A 0-size grid could spin-wait deadlock; mitigated upstream by the Task-1 `GRULayer.forward` guard (rejects before any kernel launch). Residual risk is a caller invoking a `gru_scan*` wrapper directly — the wrappers keep their existing `assert`-based shape validation; a bd issue is filed (Task 1) noting `assert` is stripped under `python -O`, but hardening that internal API is explicitly out of EDG-04 scope. |
</threat_model>

<verification>
- `uv run pytest tests/test_edge_cases.py -q` exits 0 (all non-slow edge tests; Triton cases skip cleanly on CPU-only hosts).
- `uv run pytest tests/test_edge_cases.py -m slow -q` exits 0 (long-T tier).
- `uv run pytest -q -m "not slow"` exits 0 (no existing test regressed by the Task-1 guard).
- `uv run ruff check src tests` and `uv run mypy` exit 0.
- `.planning/phases/06-edge-case-sweeps/06-pytest-output.txt` exists, non-empty, timestamped.
- No `@pytest.mark.xfail` token anywhere in the Phase 6 surface.
- D-51 locked files unchanged: `git diff --name-only` shows none of `tests/test_parity.py`, `tests/test_layer_parity.py`, `tests/test_structure.py`, or the 4 `tests/test_triton_*_strict.py` files modified.
</verification>

<success_criteria>
ROADMAP § Phase 6 success criteria 1-5, all measurable:
1. `tests/test_edge_cases.py` covers T=1 fwd+bwd for every available path at the path's normal tolerance tier (EDG-01). ✓ Task 2
2. Same file covers B=1 and H in {1,2} for every path — explicitly the CONCERNS.md BLOCK-size failure modes (EDG-02). ✓ Task 3
3. T in {512,1024} long-sequence tests are `@pytest.mark.slow`, no drift beyond tier-A tolerance (EDG-03). ✓ Task 4
4. T=0 and B=0 raise a clear `ValueError` naming the offending dimension on every path; no NaN, no hang; policy logged in PROJECT.md (EDG-04). ✓ Task 1
5. Any mismatch surfaced becomes a failing test → bd issue → in-phase fix; no `@pytest.mark.xfail` (D-04/D-05). ✓ all tasks (Commit A / bd / Commit B discipline)
</success_criteria>

<output>
After completion, create `.planning/phases/06-edge-case-sweeps/06-01-SUMMARY.md`
documenting: whether the T=0/B=0 guard was needed and its placement in
`GRULayer.forward`; any BLOCK-size bug surfaced by the B=1/H-small sweep and its bd
issue + fix commit (and, if a deep kernel fix forced a handoff, the recoverable
handoff state); the final green suite state; and a pointer to `06-pytest-output.txt`.
</output>
