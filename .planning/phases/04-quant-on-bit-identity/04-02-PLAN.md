---
phase: 04-quant-on-bit-identity
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - tests/test_triton_scan_strict.py
autonomous: true
requirements: [QNT-01, QNT-03]

must_haves:
  truths:
    - "`tests/test_triton_scan_strict.py` Phase 4 section is extended with `test_scan_quant_fwd[cls-T-B-H]` parametrized over `QUANT_FAST_GRID` (18 cases) × 3 adversarial classes (54 cases total)."
    - "Same file extended with `test_scan_quant_bwd[cls-T-B-H]` over the same parametrize axes (54 bwd cases). Bwd asserts on each of dx, dh0, dWh_cat, dbh_cat with the disposition-resolved bound."
    - "Slow-tier siblings `test_scan_quant_fwd_slow` / `test_scan_quant_bwd_slow` exist parametrized over `QUANT_SLOW_GRID` × 3 classes = 27 slow cases per direction; marked `@pytest.mark.slow`."
    - "Test bodies invoke Plan 04-01's `_make_dense_layer_quant_int8` helper (which produces a layer with the ACTUAL D-41 recipe — frozen INT8 per-channel weight + per-tensor input_act + per-tensor hidden, via inline calibrate-then-freeze). Test bodies MUST apply `xq = layer.cell.quant_x(tri_x)` BEFORE `F.linear(xq, Wi_cat, bi_cat)` so the Triton path's `gi` matches the reference's quantized-input projection. Test bodies MUST extract Triton-side `h_T` as `tri_out[-1]` since `gru_scan` returns only `out` (per `src/gru_qat/triton_kernels/scan.py:1569-1586`)."
    - "Assertion idiom matches the Plan 04-01 checkpoint:human-verify disposition resolved in `.planning/phases/04-quant-on-bit-identity/04-DISPOSITION.md` — Result A: `torch.equal`, Result B: `abs_diff < h_scale`, Result C: `abs_diff < h_scale` PLUS docstring note + bd issue referencing Phase-7 kernel scope. EXACT idiom is `[disposition: TBD per Plan 04-01 checkpoint]` — executor reads 04-DISPOSITION.md at Task 1 start and applies uniformly."
    - "Failure messages on every parametrized test include the adversarial class name (e.g., `cls=near-saturation`), the (T, B, H) shape, the max abs diff, and the h_scale per CONTEXT D-46."
    - "The `cls` parameter is the OUTERMOST `@pytest.mark.parametrize` so pytest IDs read `[realistic-8-4-32]` and the class name surfaces in `pytest -ra`."
    - "Plan 04-01 probe test `test_dense_quant_probe_bit_identity` REMAINS in the file unchanged (it's the regression detector for the original gate condition; do not delete or rename)."
    - "Phase 2 fp32 sections of `tests/test_triton_scan_strict.py` UNCHANGED (D-52). Phase 4 helpers + grid constants from Plan 04-01 UNCHANGED."
    - "D-51 locked files unchanged: `git diff <phase-4-base>..HEAD -- tests/test_parity.py tests/test_layer_parity.py tests/test_structure.py` is EMPTY."
    - "No `@pytest.mark.xfail` introduced anywhere in `tests/test_triton_scan_strict.py` (D-50)."
  artifacts:
    - path: "tests/test_triton_scan_strict.py"
      provides: "Full quant-on dense kernel parametrized test suite (fwd + bwd × adversarial × shape grid). Probe test from Plan 04-01 unchanged."
      contains: "def test_scan_quant_fwd|def test_scan_quant_bwd|def test_scan_quant_fwd_slow|def test_scan_quant_bwd_slow"
  key_links:
    - from: ".planning/phases/04-quant-on-bit-identity/04-DISPOSITION.md"
      to: "tests/test_triton_scan_strict.py assertion idiom in test_scan_quant_fwd / _bwd"
      via: "Executor reads 04-DISPOSITION.md at task start; chooses torch.equal OR abs_diff < h_scale uniformly"
      pattern: "(torch.equal|abs.\\(\\)\\.max\\(\\)\\.item\\(\\)\\s*<\\s*h_scale)"
    - from: "tests/test_triton_scan_strict.py Phase 4 helpers (Plan 04-01)"
      to: "test_scan_quant_fwd / _bwd bodies"
      via: "Reuses _make_dense_layer_quant_int8 (actual D-41 recipe), _adversarial_inputs, QUANT_FAST_GRID, QUANT_SLOW_GRID"
      pattern: "_make_dense_layer_quant_int8|_adversarial_inputs|QUANT_FAST_GRID|QUANT_SLOW_GRID"
    - from: "Plan 04-01 probe input-quantization pattern (xq = layer.cell.quant_x(tri_x); gi = F.linear(xq, Wi_cat, bi_cat))"
      to: "test_scan_quant_fwd / _bwd Triton-path setup"
      via: "Mirrors the probe; same xq-then-F.linear order. Necessary because D-41's recipe quantizes input_act and the reference path runs quant_x inside cell.step()"
      pattern: "quant_x\\(tri_x\\)|quant_x\\(.*\\)\\s*\\n.*F\\.linear"
---

<objective>
**SKETCH — assertion idiom is TBD per Plan 04-01 checkpoint.** Once the orchestrator unblocks Wave 2, this plan is filled-in with the resolved disposition from `.planning/phases/04-quant-on-bit-identity/04-DISPOSITION.md`.

Extend the Plan 04-01 Phase 4 section in `tests/test_triton_scan_strict.py` with the full quant-on parity suite for the dense kernel:
- `test_scan_quant_fwd` and `_bwd` parametrized over `QUANT_FAST_GRID` (18 cases per D-49) × 3 adversarial classes per D-46 (`realistic` / `near-saturation` / `large-magnitude`).
- Slow-tier siblings `_slow` (`QUANT_SLOW_GRID` = 9 cases) marked `@pytest.mark.slow`.

Plan 04-01's probe test STAYS in the file as the canonical gate regression. This plan adds the sweep on top of it.

Covers QNT-01 (dense quant-on bit-identity, fwd) and QNT-03 (dense quant-on bit-identity, bwd) for the dense kernel only.

Purpose: convert the empirical probe (Plan 04-01) into the full audit grid.

Output: 2 new parametrized tests + 2 `_slow` siblings = 4 new test functions × Cartesian-product parametrize = ~162 cases (54 fast × 3 directions; the slow cases collapse to 27 each).

**This plan is autonomous** — no checkpoint. The Plan 04-05 GPU run is the validation gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-quant-on-bit-identity/04-CONTEXT.md
@.planning/phases/04-quant-on-bit-identity/04-PATTERNS.md
@.planning/phases/04-quant-on-bit-identity/04-01-PLAN.md
@.planning/phases/04-quant-on-bit-identity/04-DISPOSITION.md
@CLAUDE.md

<interfaces>
<!-- Phase 4 helpers from Plan 04-01 — already in tests/test_triton_scan_strict.py -->

From `tests/test_triton_scan_strict.py` (post-Plan-04-01):
```
def _make_dense_layer_quant_int8(in_dim: int, hidden: int, h_scale: float = 0.02) -> GRULayer:
    # Returns a layer with the ACTUAL D-41 recipe:
    #   - weight:    bits=8, axis=0, frozen (per-channel; inline calibrate-then-freeze)
    #   - input_act: bits=8, frozen (per-tensor; inline calibrate-then-freeze)
    #   - hidden:    bits=8, frozen (per-tensor; manually set to h_scale)
    # End state: every quantizer in mode='frozen' after cell.freeze_quantizers().

def _adversarial_inputs(
    cls: str, T: int, B: int, H: int, device: torch.device, h_scale: float = 0.02
) -> tuple[torch.Tensor, torch.Tensor]: ...
    # cls ∈ {"realistic", "near-saturation", "large-magnitude"}

QUANT_FAST_GRID: list[tuple[int, int, int]]   # 18 cases — (T, B, H) for T ∈ {8,64}, B ∈ {1,4,32}, H ∈ {32,128,512}
QUANT_SLOW_GRID: list[tuple[int, int, int]]   # 9 cases — T=512 only

def test_dense_quant_probe_bit_identity() -> None: ...   # KEEP UNCHANGED
```

From `src/gru_qat/triton_kernels/scan.py`:
```
def gru_scan(gi, h0, Wh_cat, bh_cat, *, h_in_quant=None, h_out_quant=None) -> torch.Tensor: ...
    # Returns a SINGLE tensor `out` of shape [T, B, H]. h_T is NOT returned —
    # the caller extracts it as out[-1]. (Verified at src/gru_qat/triton_kernels/scan.py:1569-1586.)
```

From `src/gru_qat/gru_layer.py`:
```
GRULayer.forward(x, h0) -> tuple[torch.Tensor, torch.Tensor]   # (out, h_T) where h_T = out[-1]
```

From `src/gru_qat/gru_cell.py:240-272`:
```
GRUCellQuant.quantize_weights() -> CellWeights
    # gate_layout='fused' produces Wi_cat = cat([Wir,Wiz,Win], dim=0) and
    # Wh_cat = cat([Whr,Whz,Whn], dim=0). Reference per-row gradients live on
    # layer.cell.W_hr.grad / W_hz / W_hn (and similarly for biases on b_hr/_hz/_hn);
    # concat in the SAME order ([r, z, n]) for the bwd parity comparison.
```

From `tests/test_triton_scan.py:213-389` (canonical QAT-frozen-INT8 fwd + bwd pattern — primary analog for the Phase 4 sweep bodies; differs in recipe: the analog used `bits=32` weights, Phase 4 uses the actual D-41 INT8 recipe via the Plan 04-01 helper).

From `.planning/phases/04-quant-on-bit-identity/04-DISPOSITION.md` (written by Plan 04-01 checkpoint):
- Result A: `torch.equal(ref, tri)` is the assertion.
- Result B: `(ref - tri).abs().max().item() < h_scale` is the assertion.
- Result C: same as Result B + docstring referencing the deferred kernel-change bd issue.

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read 04-DISPOSITION.md and add Cartesian-parametrized test_scan_quant_fwd / _bwd</name>
  <files>tests/test_triton_scan_strict.py</files>
  <read_first>
    - **.planning/phases/04-quant-on-bit-identity/04-DISPOSITION.md** (MANDATORY — contains D-42 resolution from Plan 04-01 checkpoint:human-verify; determines the assertion idiom for the entire plan)
    - tests/test_triton_scan_strict.py (full — Phase 2 fp32 sections + Phase 4 section from Plan 04-01)
    - tests/test_triton_scan.py lines 213-389 (canonical QAT-fwd + QAT-bwd pattern with `gru_scan` / `gru_scan_persistent` + `h_in_q` / `h_out_q` tuples; the Phase 4 fwd/bwd bodies mirror these but with the disposition-resolved bound and adversarial-class parametrize; recipe differs — see Plan 04-01 helper)
    - tests/test_triton_scan_strict.py Plan 04-01 probe body (the analog for `quant_x(tri_x)`-then-`F.linear` order and `tri_hT = tri_out[-1]` extraction)
    - .planning/phases/04-quant-on-bit-identity/04-PATTERNS.md (full — especially "Per-adversarial-class parametrize idiom", "torch.equal vs abs_diff < scale_h assertion idiom", and "Input activation quantization BEFORE the F.linear projection" sections)
    - .planning/phases/04-quant-on-bit-identity/04-CONTEXT.md (D-43 / D-46 / D-49)
  </read_first>
  <behavior>
    - Read 04-DISPOSITION.md FIRST. Extract the resolved disposition (Result A/B/C).
    - Build the assertion idiom as a single helper function `_assert_quant_parity(ref, tri, *, name, cls, T, B, H, h_scale)` defined once at the top of the Phase 4 section (right after the `_adversarial_inputs` helper from Plan 04-01). The helper picks the assertion form per disposition:
      - Result A: `assert torch.equal(ref, tri), f"{name}: torch.equal failed for cls={cls} (T={T},B={B},H={H}); max abs diff {(ref-tri).abs().max().item():.4e}; h_scale={h_scale}"`
      - Result B / C: `max_diff = (ref - tri).abs().max().item(); assert max_diff < h_scale, f"{name}: max abs diff {max_diff:.4e} > h_scale ({h_scale}) for cls={cls} (T={T},B={B},H={H})"`
    - This centralizes the disposition switch — no per-test conditional. If the disposition changes mid-phase (e.g., user revises at Plan 04-05), only `_assert_quant_parity` changes.
    - Add `test_scan_quant_fwd[cls-T-B-H]`: parametrize over `cls ∈ {"realistic", "near-saturation", "large-magnitude"}` (OUTERMOST) × `(T, B, H) ∈ QUANT_FAST_GRID`. Body mirrors Plan 04-01's probe shape:
      1. Build `layer = _make_dense_layer_quant_int8(IN=H, H).to(device).eval()` (the Plan 04-01 helper returns a fully-frozen actual-D-41-recipe layer; per-channel weight scales come from the QNT-04-fixed `cell.freeze_quantizers()` path).
      2. Build inputs via `_adversarial_inputs(cls, T, B, H, device)`.
      3. Run reference: `ref_out, ref_hT = layer(ref_x, ref_h0)`.
      4. Extract weights via `w = layer.cell.quantize_weights()` and detach-clone `Wi_cat`, `bi_cat`, `Wh_cat`, `bh_cat`.
      5. Triton-side input quantization: `xq = layer.cell.quant_x(tri_x)` BEFORE `gi = F.linear(xq, Wi_cat, bi_cat)`. (D-41's recipe quantizes input_act; the reference runs quant_x inside `cell.step()`.)
      6. Call `tri_out = gru_scan(gi, tri_h0, Wh_cat, bh_cat, h_in_quant=(h_scale, -127, 127), h_out_quant=(h_scale, -127, 127))`.
      7. Extract `tri_hT = tri_out[-1]` (because `gru_scan` returns only `out`).
      8. Call `_assert_quant_parity` for `(out, h_T)` — 2 assertions per fwd test, both naming the tensor in the failure message.
    - Add `test_scan_quant_bwd[cls-T-B-H]`: same parametrize. Body builds layer + inputs, runs reference fwd+bwd with `ref_out.float().pow(2).sum().backward()`, captures reference grads from the cell leaves:
      - `ref_dx = ref_x.grad`, `ref_dh0 = ref_h0.grad`
      - `ref_dWh_cat = torch.cat([layer.cell.W_hr.grad, layer.cell.W_hz.grad, layer.cell.W_hn.grad], dim=0)`
      - `ref_dbh_cat = torch.cat([layer.cell.b_hr.grad, layer.cell.b_hz.grad, layer.cell.b_hn.grad])`
      Then runs Triton fwd+bwd with `tri_out.float().pow(2).sum().backward()`, captures `tri_dx = tri_x.grad`, `tri_dh0 = tri_h0.grad`, `tri_dWh_cat = Wh_cat.grad`, `tri_dbh_cat = bh_cat.grad`. Calls `_assert_quant_parity` independently on each of (dx, dh0, dWh_cat, dbh_cat) — 4 assertions per bwd test; failure on any one names the offending gradient.
    - Add `test_scan_quant_fwd_slow` / `_bwd_slow` parametrized over `QUANT_SLOW_GRID` × the 3 classes; `@pytest.mark.slow` decorator.
    - All 4 new test functions decorated with `@cuda_only`.
    - **DO NOT** modify Plan 04-01's probe test, helpers, or grid constants. Append the new content INSIDE the Phase 4 section, after the probe test.
  </behavior>
  <action>
    1. Read `.planning/phases/04-quant-on-bit-identity/04-DISPOSITION.md`. If the file does not exist OR the disposition is `findings`/`defer`, halt the task and surface to orchestrator — Plan 04-02 cannot proceed without a resolved disposition.
    2. Open `tests/test_triton_scan_strict.py`. Locate the end of the Phase 4 section (after Plan 04-01's `test_dense_quant_probe_bit_identity`). All new content appends here.
    3. Append the `_assert_quant_parity` helper per behavior block. Include docstring citing 04-DISPOSITION.md's resolved Result, the bd issue reference (if Result B or C), and the assertion-idiom switch.
    4. Append `test_scan_quant_fwd` per behavior block. Body structure mirrors Plan 04-01's probe (same input-quant order, same `tri_hT = tri_out[-1]` extraction). Swap the bound for `_assert_quant_parity` and use cls-parametrized adversarial inputs.
    5. Append `test_scan_quant_bwd` per behavior block. Reference-grad extraction uses the `torch.cat([W_hr.grad, W_hz.grad, W_hn.grad], dim=0)` pattern (matches Plan 04-01's probe — same concat order as `quantize_weights()`'s axis=0 per `gru_cell.py:268`). Per-gradient assertion — 4 separate `_assert_quant_parity` calls so failure messages name the offending tensor.
    6. Append `test_scan_quant_fwd_slow` and `_bwd_slow` with `@pytest.mark.slow` and `QUANT_SLOW_GRID`.
    7. Run `pytest --collect-only` on CPU. Expected: 162 quant-on cases collected (3 cls × 18 fast × 2 dirs = 108 fast + 3 × 9 × 2 = 54 slow). All SKIP on CPU via `@cuda_only`.
    8. Hygiene gates:
       - `grep -r "xfail" tests/test_triton_scan_strict.py` → empty (D-50).
       - `git diff HEAD -- tests/test_triton_scan_strict.py | grep "^-" | grep -v "^---"` → empty (no deletions; Phase 2 + Plan 04-01 content unchanged).
       - `git diff HEAD -- tests/test_parity.py tests/test_layer_parity.py tests/test_structure.py` → empty (D-51).
       - `grep "quant_x" tests/test_triton_scan_strict.py | wc -l` → ≥ 3 (probe + 2 parametrized tests; verify the input-quantization order is applied uniformly).
       - `grep "tri_out\[-1\]\|out\[-1\]" tests/test_triton_scan_strict.py | wc -l` → ≥ 3 (probe + fwd parametrized + bwd parametrized; h_T extraction is uniform).
    9. Commit. Stage ONLY `tests/test_triton_scan_strict.py`. Commit message:
       ```
       test(04-02): dense kernel quant-on full sweep per D-49 + D-46

       Extends the Phase 4 section in tests/test_triton_scan_strict.py with the
       full quant-on parity grid for the dense Triton kernel:
       - _assert_quant_parity helper (disposition-aware: <Result-X from 04-DISPOSITION.md>)
       - test_scan_quant_fwd parametrized over 3 adversarial classes × QUANT_FAST_GRID
         (18 cases) = 54 fast cases; asserts on (out, h_T) per parametrize case.
         tri_hT = tri_out[-1] since gru_scan returns only `out`.
       - test_scan_quant_bwd same, with per-gradient (dx, dh0, dWh_cat, dbh_cat)
         assertions = 54 fast cases. Reference dWh_cat / dbh_cat extracted via
         torch.cat of layer.cell.W_h{r,z,n}.grad in axis=0 order (matches
         quantize_weights' concat order).
       - _slow siblings over QUANT_SLOW_GRID (9 cases × 3 = 27 each direction)

       Triton path applies layer.cell.quant_x(tri_x) BEFORE F.linear because
       D-41's actual recipe (in Plan 04-01 helper) quantizes input_act.

       Disposition per D-42 resolved at Plan 04-01 checkpoint:
       <quote from 04-DISPOSITION.md>.

       Plan 04-01 probe test_dense_quant_probe_bit_identity unchanged.
       Phase 2 fp32 sections unchanged (D-52). Locked files unchanged (D-51).
       No xfails (D-50).

       Covers QNT-01 (dense fwd), QNT-03 (dense bwd).
       ```
  </action>
  <verify>
    <automated>cd /home/claroche/gru-triton && uv run pytest tests/test_triton_scan_strict.py --collect-only -q 2>&1 | grep -E "test_scan_quant_(fwd|bwd)" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_triton_scan_strict.py` contains 4 new test functions: `test_scan_quant_fwd`, `test_scan_quant_bwd`, `test_scan_quant_fwd_slow`, `test_scan_quant_bwd_slow`.
    - All 4 test functions are decorated with `@cuda_only`. Slow variants have `@pytest.mark.slow`.
    - Cartesian parametrize: `cls` is the OUTERMOST parametrize (pytest IDs read `[realistic-8-4-32]`).
    - `pytest --collect-only` reports at least 162 quant-on test items (54 fast × 2 dirs + 27 slow × 2 dirs = 162).
    - `_assert_quant_parity` helper exists once, at the top of the Phase 4 section, and the assertion idiom matches the disposition resolved in `.planning/phases/04-quant-on-bit-identity/04-DISPOSITION.md`.
    - Each new test body calls `layer.cell.quant_x(tri_x)` before `F.linear(...)`; verifiable via `grep "quant_x" tests/test_triton_scan_strict.py` showing ≥ 3 occurrences (probe + fwd + bwd).
    - Each new fwd test body extracts `tri_hT = tri_out[-1]` (NOT a tuple destructure of `gru_scan` return).
    - Plan 04-01's `test_dense_quant_probe_bit_identity`, `_make_dense_layer_quant_int8`, `_adversarial_inputs`, `QUANT_FAST_GRID`, `QUANT_SLOW_GRID` are byte-for-byte unchanged.
    - Phase 2 fp32 sections (lines 1-446 of the original file) are byte-for-byte unchanged.
    - `grep -r xfail tests/test_triton_scan_strict.py` returns empty.
    - `git diff` over locked files (test_parity.py, test_layer_parity.py, test_structure.py) returns empty.
    - On CPU, all 162 new tests SKIP cleanly.
  </acceptance_criteria>
  <done>
    Dense kernel quant-on full grid is in the strict-tier test file. CUDA validation deferred to Plan 04-05.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 04-DISPOSITION.md ↔ assertion idiom | The disposition file is the SOLE source of truth for whether `torch.equal` or `abs_diff < h_scale` is used. Misreading it cascades into Plans 04-03..04 (same idiom must apply uniformly per D-43). |
| Phase 4 section append ↔ Phase 2 fp32 sections | Verifier asserts no deletions/modifications to Phase 2 content (D-52) |
| Plan 04-01 helper ↔ Plan 04-02 test bodies | Test bodies trust the helper to return a frozen-INT8 layer per D-41; the helper's `cell.freeze_quantizers()` step depends on the QNT-04 fix from Plan 04-01 Task 3. Test bodies MUST NOT bypass the helper (e.g., by building a Plain `bits=32` recipe inline). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | Plan 04-02 modifies Plan 04-01 helpers or probe | mitigate | Acceptance criteria: probe + helpers byte-for-byte unchanged; `git diff` over Plan-04-01-added lines shows zero deletions |
| T-04-09 | Repudiation | Disposition idiom drifts between Plan 04-02 and Plans 04-03..04 (D-43 violation) | mitigate | All Phase 4 plans read the SAME `.planning/phases/04-quant-on-bit-identity/04-DISPOSITION.md` and use the SAME `_assert_quant_parity` shape; Plan 04-05 verifier scans all four strict files for the same helper signature |
| T-04-10 | Tampering | Locked Phase 2 / Phase 1 / Phase 3 files modified during Phase 4 | mitigate | `git diff` over locked files asserted in acceptance criteria (D-51) |
| T-04-32 | Tampering | Triton-path test body forgets `quant_x(tri_x)` before `F.linear` — silent recipe mismatch | mitigate | Acceptance: `grep "quant_x" tests/test_triton_scan_strict.py | wc -l` ≥ 3 (probe + fwd + bwd); cross-reference all bodies share the same input-quant-then-F.linear order |
| T-04-33 | Tampering | Triton-path fwd test body tuple-destructures `gru_scan` return (`tri_out, tri_hT = gru_scan(...)` would TypeError) | mitigate | Acceptance: `grep "tri_out\[-1\]"` count ≥ 3 across probe + fwd + bwd; collect-only run catches TypeError at import time |

</threat_model>

<verification>
After this plan:
- 162 new quant-on test items in `tests/test_triton_scan_strict.py`, all skipping on CPU and ready for CUDA validation in Plan 04-05.
- Phase 4 disposition uniformly applied via single `_assert_quant_parity` helper.
- Test bodies apply input-quantization-before-F.linear and extract h_T = out[-1] uniformly.
- All D-50, D-51, D-52 hygiene gates green.
</verification>

<success_criteria>
- All must-have truths verified.
- Plan 04-02 lands a single atomic commit modifying ONLY `tests/test_triton_scan_strict.py`.
- Disposition correctly applied per 04-DISPOSITION.md.
</success_criteria>

<output>
After completion:
- `tests/test_triton_scan_strict.py` extended with full dense quant-on grid.
- `.planning/phases/04-quant-on-bit-identity/04-02-SUMMARY.md` written by execute-plan.

Wave 2 sibling plans (04-03, 04-04) run in parallel against disjoint strict files.
</output>
