---
phase: 04-quant-on-bit-identity
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - tests/test_triton_butterfly_strict.py
autonomous: true
requirements: [QNT-02, QNT-03]

must_haves:
  truths:
    - "`tests/test_triton_butterfly_strict.py` is extended with a Phase 4 section: `_make_butterfly_layer_quant_int8(H, *, use_triton, h_scale)` helper that wraps the existing `_make_layer(H, use_triton, hidden_bits=8)` helper at lines ~86-100, then forces all quantizer modes to produce the ACTUAL D-41 recipe (weight/input_act → `mode='min_max'`; hidden → `mode='frozen'` with manually-set `h_scale`), runs ONE inline calibration forward, and calls `layer.cell.freeze_quantizers()` to land per-channel weight scales + per-tensor input_act scales."
    - "Section adds `test_butterfly_quant_fwd` + `_bwd` parametrized over `QUANT_FAST_GRID` (18 cases × 3 adversarial classes = 54 per direction) and `_slow` siblings under `QUANT_SLOW_GRID` (9 × 3 = 27)."
    - "Butterfly comparator pattern uses DUAL-LAYER (per Phase 2 strict pattern at `tests/test_triton_butterfly_strict.py:138-156` / 04-PATTERNS.md): `pt_layer = _make_butterfly_layer_quant_int8(H, use_triton=False)` vs `fast_layer = _make_butterfly_layer_quant_int8(H, use_triton=True)`, plus `fast_layer.load_state_dict(pt_layer.state_dict())` to share weights AND frozen scales (state_dict includes `scale`/`zero_point`/`running_min`/`running_max` buffers since they're registered via `register_buffer` in `FakeQuantize.__init__` at `src/gru_qat/quantizers.py:80-83`)."
    - "Dual-layer test bodies call `layer(x, h0)` (the high-level forward) rather than `gru_scan_butterfly_forward_triton` directly. `layer.forward()` runs `quant_x` internally (per D-41 input_act recipe) so the input-quantization-before-linear order is enforced by the layer itself — no extra `quant_x(x)` call needed in the test body."
    - "Local `_assert_quant_parity` helper matches Plan 04-02/04-03 idiom byte-for-byte (D-43 uniformity); disposition resolved per `.planning/phases/04-quant-on-bit-identity/04-DISPOSITION.md`."
    - "Local `_adversarial_inputs` helper matches Plan 04-01 byte-for-byte."
    - "H values restricted to `{32, 128, 512}` (powers of 2 satisfied by D-49 implicitly; QUANT_FAST_GRID already complies)."
    - "Butterfly bwd uses autograd-through-`layer.forward()` (no closed-form `_pytorch` analog; per 04-PATTERNS.md caveat). Per-grad assertions on `dx`, `dh0`, and any cell-leaf gradients that survive the dual-layer comparison (e.g., struct_Wh_* twiddle parameters); name each asserted gradient in the failure message."
    - "Phase 2 fp32 strict-tier sections of `tests/test_triton_butterfly_strict.py` UNCHANGED (D-52). Plan 04-01 Phase 4 dense section UNCHANGED. Plan 04-03 diagonal/monarch sections UNCHANGED."
    - "D-22 OOB regression at `tests/test_butterfly_dispatch.py:164` is NOT modified or duplicated — it remains where it is. Phase 4 cites it in the docstring."
    - "D-51 locked files unchanged. D-50 no xfail. `pytest.importorskip(\"torch_structured\")` preserved."
  artifacts:
    - path: "tests/test_triton_butterfly_strict.py"
      provides: "Butterfly kernel quant-on parity sweep (fwd + bwd) with dual-layer comparator pattern; per-grad assertions; actual D-41 recipe via wrapper around _make_layer + cell.freeze_quantizers()."
      contains: "def _make_butterfly_layer_quant_int8|def test_butterfly_quant_fwd|def test_butterfly_quant_bwd|freeze_quantizers|load_state_dict"
  key_links:
    - from: ".planning/phases/04-quant-on-bit-identity/04-DISPOSITION.md"
      to: "Butterfly quant-on assertion idiom"
      via: "_assert_quant_parity helper uniform with Plans 04-02/04-03 (D-43)"
      pattern: "_assert_quant_parity"
    - from: "tests/test_butterfly_dispatch.py:369-407 (Phase 2 realistic-tier QAT analog)"
      to: "tests/test_triton_butterfly_strict.py Phase 4 section bodies"
      via: "h_q tuple plumbing into gru_scan_butterfly_forward_triton (via _make_layer's use_triton arg) + dual-layer comparator"
      pattern: "gru_scan_butterfly_forward_triton|load_state_dict"
    - from: "tests/test_triton_butterfly_strict.py:86-100 (existing _make_layer helper, supports hidden_bits=8)"
      to: "_make_butterfly_layer_quant_int8 wrapper"
      via: "Wrapper calls _make_layer(H, use_triton, hidden_bits=8), overrides quantizer modes to actual D-41 recipe, runs inline calibration, calls cell.freeze_quantizers()"
      pattern: "_make_layer\\(H, use_triton, hidden_bits=8\\)"
    - from: "FakeQuantize buffer registration (running_min/max/scale/zero_point) at src/gru_qat/quantizers.py:80-83"
      to: "load_state_dict propagation of frozen scales"
      via: "Buffers participate in state_dict; load_state_dict copies them verbatim. Verified by early `torch.equal(pt_layer.cell.quant_h_in.scale, fast_layer.cell.quant_h_in.scale)` assertion in each test body."
      pattern: "load_state_dict.*state_dict|quant_h_in.scale"
---

<objective>
**SKETCH — assertion idiom is TBD per Plan 04-01 checkpoint.** Filled in after 04-DISPOSITION.md lands.

Extend `tests/test_triton_butterfly_strict.py` with a Phase 4 section per D-47/D-48. Butterfly is the last of the four Triton kernels; it uses a slightly different test pattern from dense/diagonal/monarch because there's no pure-PyTorch reference distinct from the kernel under test — the comparator is `use_triton=False` (CUDA-op per-step path via `butterfly_multiply` from `torch_structured`).

Covers QNT-02 (butterfly fwd bit-identity) and QNT-03 (butterfly bwd gradients), completing the QNT-02 requirement (diagonal + monarch + butterfly).

Purpose: convert the empirical probe disposition (Plan 04-01) into the full butterfly audit grid.

Output: 1 file extension; ~108 new fast cases + 54 slow cases.

**This plan is autonomous** — no checkpoint. The Plan 04-05 CUDA GPU run validates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-quant-on-bit-identity/04-CONTEXT.md
@.planning/phases/04-quant-on-bit-identity/04-PATTERNS.md
@.planning/phases/04-quant-on-bit-identity/04-01-PLAN.md
@.planning/phases/04-quant-on-bit-identity/04-02-PLAN.md
@.planning/phases/04-quant-on-bit-identity/04-DISPOSITION.md
@CLAUDE.md

<interfaces>
<!-- Butterfly kernel + per-step comparator -->

From `src/gru_qat/triton_kernels/scan_butterfly.py`:
```
def gru_scan_butterfly_forward_triton(
    gi, h0, twiddles, bh_cat, *, h_in_quant=None, h_out_quant=None
) -> torch.Tensor: ...
```

From `tests/test_triton_butterfly_strict.py` (existing Phase 2 strict file):
```
def _make_layer(H: int, *, use_triton: bool = True, hidden_bits: int = 32) -> GRULayer: ...
    # at ~line 86-100; already supports hidden_bits=8 — Phase 4 wraps this
    # The default recipe has bits=8 weight + bits=8 input_act + bits=<hidden_bits>
    # hidden, all in mode='dynamic'. Phase 4 wrapper overrides modes to land
    # the actual D-41 frozen recipe.
def _assert_grad_close(...): ...   # at ~line 190-211; helper for per-grad assertion
# FAST_BUTTERFLY_GRID, SLOW_BUTTERFLY_GRID — Phase 2 grids; DO NOT reuse for Phase 4.
```

From `tests/test_butterfly_dispatch.py:369-407` (canonical QAT-fwd analog):
```
# Pattern: build dual layers (pt + fast), share state_dict, run both, compare with h_q tuple
pt_layer = _make_layer(H, use_triton=False, hidden_bits=8)
fast_layer = _make_layer(H, use_triton=True, hidden_bits=8)
fast_layer.load_state_dict(pt_layer.state_dict())
# Then call layer(x, h0) on each side; compare outputs.
```

From `tests/test_triton_butterfly_strict.py:138-156` (Phase 2 strict dual-layer pattern):
```
# Phase 2 strict pattern: both layers use the same _make_layer, share state_dict.
# Phase 4 follows this pattern but with hidden_bits=8 + actual D-41 frozen
# recipe (override modes + inline cell.freeze_quantizers()).
```

From `src/gru_qat/quantizers.py`:
```
FakeQuantizePerTensor.scale: torch.Tensor   # registered buffer; reassignable
FakeQuantizePerTensor.zero_point: torch.Tensor
FakeQuantizePerTensor.config.mode: Literal["dynamic", "min_max", "frozen"]
# All these participate in state_dict via register_buffer at lines 80-83.
```

From `src/gru_qat/gru_cell.py`:
```
GRUCellQuant.freeze_quantizers() -> None     # lines 497-505; switches all
                                             # observer-mode (min_max) quantizers
                                             # to frozen mode using running stats.
```

From `.planning/phases/04-quant-on-bit-identity/04-DISPOSITION.md`:
- Same Result A/B/C disposition; `_assert_quant_parity` idiom uniform with Plans 04-02/04-03.

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Butterfly kernel — Phase 4 section extension</name>
  <files>tests/test_triton_butterfly_strict.py</files>
  <read_first>
    - .planning/phases/04-quant-on-bit-identity/04-DISPOSITION.md (MANDATORY)
    - tests/test_triton_butterfly_strict.py (full — Phase 2 file; especially _make_layer at 86-100, _assert_grad_close at 190-211, dual-layer pattern at 138-156)
    - tests/test_butterfly_dispatch.py lines 369-462 (QAT fwd + calibrate-freeze analog; dual-layer pattern)
    - tests/test_triton_diagonal_strict.py post-Plan-04-03 (for `_assert_quant_parity` byte-for-byte reference)
    - .planning/phases/04-quant-on-bit-identity/04-PATTERNS.md (full — butterfly section with the revised wrapper body + "Pattern caveat — butterfly comparator")
    - .planning/phases/04-quant-on-bit-identity/04-CONTEXT.md (D-43 / D-46 / D-47 / D-48 / D-49 / D-52)
  </read_first>
  <behavior>
    - Read 04-DISPOSITION.md. Halt if missing / unresolved.
    - Append Phase 4 section at the END of `tests/test_triton_butterfly_strict.py`. Phase 2 fp32 content unchanged (D-52). Existing `_assert_grad_close` helper at line ~190-211 stays (Phase 2 file's bwd helper) — Phase 4 builds a separate `_assert_quant_parity` helper specifically for the disposition idiom.
    - Section header per D-48.
    - Section contents:
      1. Local copy of `_assert_quant_parity` helper (byte-for-byte identical to Plans 04-02/04-03).
      2. `_make_butterfly_layer_quant_int8(H, *, use_triton, h_scale=0.02) -> GRULayer` per 04-PATTERNS.md (revised wrapper body). Wraps existing `_make_layer(H, use_triton, hidden_bits=8)`, then:
         - For each of `quant_W_ir/_iz/_in/_hr/_hz/_hn`: set `q.config.mode = "min_max"`. (Skip any that are None for structured-input cells.)
         - For `quant_x`: set `quant_x.config.mode = "min_max"`.
         - For `quant_h_in` and `quant_h_out`: set `config.mode = "frozen"` and manually set `q.scale = torch.tensor(h_scale)` / `q.zero_point = torch.tensor(0.0)`.
         - Inline calibration: `layer.eval(); with torch.no_grad(): layer(torch.randn(8, 4, H)*0.5, torch.randn(4, H)*0.5)`. Populates running_min/max on weight + input_act quantizers.
         - `layer.cell.freeze_quantizers()`. Switches weight + input_act quantizers from `min_max → frozen`.
      3. Local copy of `_adversarial_inputs`.
      4. `QUANT_FAST_GRID` / `QUANT_SLOW_GRID` constants per D-49. H values `{32, 128, 512}` are powers of 2 — fits butterfly's restriction.
      5. `test_butterfly_quant_fwd[cls-T-B-H]`: parametrize over cls × `QUANT_FAST_GRID`. Body uses dual-layer pattern per `tests/test_triton_butterfly_strict.py:138-156`:
         - `pt_layer = _make_butterfly_layer_quant_int8(H, use_triton=False)`
         - `fast_layer = _make_butterfly_layer_quant_int8(H, use_triton=True)`
         - `fast_layer.load_state_dict(pt_layer.state_dict())` — share weights AND frozen scales (`register_buffer` ensures `scale`/`zero_point`/`running_min`/`running_max` participate in state_dict)
         - **Early state-sharing assertion (mitigation T-04-16):** `assert torch.equal(pt_layer.cell.quant_h_in.scale, fast_layer.cell.quant_h_in.scale)`, same for `quant_h_out.scale`, AND for ONE weight quantizer's scale (e.g. `quant_W_hr.scale`) so the test catches per-channel scale mismatch too.
         - Build `x`, `h0` via `_adversarial_inputs(cls, T, B, H, device)`.
         - `pt_out, pt_hT = pt_layer(x, h0)`; `fast_out, fast_hT = fast_layer(x, h0)`. (Both go through `GRULayer.forward()`, which runs `quant_x` internally per the recipe — no extra input-quant call needed in the test body.)
         - `_assert_quant_parity(pt_out, fast_out, name="out", cls=cls, T=T, B=B, H=H, h_scale=h_scale)`.
         - h_scale extracted from `pt_layer.cell.quant_h_in.scale.item()`.
      6. `test_butterfly_quant_bwd[cls-T-B-H]`: same parametrize. Build dual layers, run backward on both with `.float().pow(2).sum().backward()`, assert per-grad via `_assert_quant_parity` on each of `dx` (`x.grad` of the leaf passed into `layer(x, h0)`), `dh0`, and the structured-weight gradients (twiddles for butterfly). Per-grad assertions named in the failure message.
      7. `_slow` siblings over `QUANT_SLOW_GRID` with `@pytest.mark.slow`.
    - All `@cuda_only`. `pytest.importorskip("torch_structured")` preserved at module top from Phase 2.
    - DO NOT modify the D-22 OOB regression in `tests/test_butterfly_dispatch.py:164` — it's a different file and is referenced in the Phase 4 section docstring, not duplicated.
  </behavior>
  <action>
    1. Read 04-DISPOSITION.md. Halt if missing or unresolved.
    2. Open `tests/test_triton_butterfly_strict.py`. Verify `pytest.importorskip("torch_structured")` at module top; preserve as-is.
    3. Find the end of the file. Append the Phase 4 section per behavior block.
    4. **DO NOT duplicate `_make_layer`** — the existing helper at line ~86-100 already accepts `hidden_bits=8`. Build `_make_butterfly_layer_quant_int8` as a thin wrapper that calls `_make_layer(H, use_triton=use_triton, hidden_bits=8)`, then overrides quantizer modes per behavior block, runs inline calibration, and calls `layer.cell.freeze_quantizers()`.
    5. Dual-layer + `load_state_dict` pattern in both fwd and bwd tests. Verify `state_dict` propagates frozen scales — scales are in registered buffers (per `FakeQuantize.__init__` lines 80-83), so `load_state_dict` copies them. Add an explicit assertion early in each test body: `assert torch.equal(pt_layer.cell.quant_h_in.scale, fast_layer.cell.quant_h_in.scale)` to catch any silent state-sharing mismatch BEFORE the quant-on parity assertion runs. Also assert on one weight quantizer's scale (e.g. `quant_W_hr.scale`) — per-channel buffer sharing is the more subtle case.
    6. Per-grad assertions in `test_butterfly_quant_bwd` name the offending tensor: `dx`, `dh0`, structured weight gradients (consult the Phase 2 file's `_assert_grad_close` callsite at line ~214-270 for the exact gradient-leaf names the butterfly bwd produces).
    7. Hygiene gates: `grep -r xfail` empty; Phase 2 section unchanged; locked files empty diff; D-22 regression untouched; `grep "freeze_quantizers" tests/test_triton_butterfly_strict.py | wc -l` ≥ 1 (helper).
    8. Commit. Stage ONLY `tests/test_triton_butterfly_strict.py`. Commit message:
       ```
       test(04-04): butterfly kernel quant-on full sweep with dual-layer comparator

       Extends tests/test_triton_butterfly_strict.py with Phase 4 section:
       - _assert_quant_parity helper (disposition: <Result-X from 04-DISPOSITION.md>)
       - _make_butterfly_layer_quant_int8 wrapper around existing _make_layer
         (line 86-100): overrides quantizer modes to land the actual D-41
         frozen recipe (weight/input_act min_max → cell.freeze_quantizers()
         after inline calibration; hidden manually frozen at h_scale)
       - _adversarial_inputs + QUANT_FAST/SLOW_GRID
       - test_butterfly_quant_fwd over cls × QUANT_FAST_GRID, dual-layer
         pattern (use_triton=False vs True + load_state_dict).
         Early-asserts pt_layer + fast_layer scales agree (catches buffer
         propagation regressions).
       - test_butterfly_quant_bwd same; per-grad (dx, dh0, twiddles, ...)
       - _slow siblings

       layer.forward() runs quant_x internally so the input-quant-before-
       linear order is enforced by the layer (no extra quant_x call in body).

       Depends on QNT-04 fix from Plan 04-01 Task 3 (per-channel weight
       freeze_quantizers requires per-channel running stats).

       D-22 OOB regression at tests/test_butterfly_dispatch.py:164 NOT modified.
       Phase 2 fp32 section unchanged (D-52). Locked files unchanged (D-51).
       No xfails (D-50). torch_structured import-skip preserved.

       Covers QNT-02 (butterfly fwd), QNT-03 (butterfly bwd).
       ```
  </action>
  <verify>
    <automated>cd /home/claroche/gru-triton && uv run pytest tests/test_triton_butterfly_strict.py --collect-only -q 2>&1 | grep -E "test_butterfly_quant_(fwd|bwd)" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_triton_butterfly_strict.py` contains 4 new test functions: `test_butterfly_quant_fwd`, `_bwd`, `_fwd_slow`, `_bwd_slow`.
    - `_make_butterfly_layer_quant_int8` is a wrapper around the existing `_make_layer(H, use_triton, hidden_bits=8)` helper — NOT a duplicate of `_make_layer`. Verifiable via `grep "_make_layer(H" tests/test_triton_butterfly_strict.py` showing the wrapper calls it.
    - Helper body switches weight + input_act quantizers to `mode="min_max"` and calls `cell.freeze_quantizers()` after one inline calibration forward. Verifiable via `grep "freeze_quantizers" tests/test_triton_butterfly_strict.py` returning ≥ 1.
    - Dual-layer pattern present in both fwd and bwd test bodies: `_make_butterfly_layer_quant_int8(H, use_triton=False)` + `_make_butterfly_layer_quant_int8(H, use_triton=True)` + `fast_layer.load_state_dict(pt_layer.state_dict())`.
    - Early-assertion: `torch.equal(pt_layer.cell.quant_h_in.scale, fast_layer.cell.quant_h_in.scale)` (and similarly for `quant_h_out` and at least one weight quantizer) — catches state-sharing mismatch before parity assertion.
    - `_assert_quant_parity` body byte-for-byte identical to Plans 04-02 and 04-03 (D-43 uniformity).
    - Phase 2 fp32 sections + existing `_assert_grad_close` helper byte-for-byte unchanged.
    - `tests/test_butterfly_dispatch.py:164` D-22 OOB regression NOT modified (`git diff -- tests/test_butterfly_dispatch.py` returns empty).
    - `grep -r xfail tests/test_triton_butterfly_strict.py` empty.
    - `git diff` over locked files empty.
    - On CPU, all new tests SKIP via `@cuda_only` or via `torch_structured` import skip.
  </acceptance_criteria>
  <done>
    Butterfly kernel quant-on full grid in the strict file. CUDA validation in Plan 04-05.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Existing `_make_layer` helper ↔ `_make_butterfly_layer_quant_int8` wrapper | The wrapper must invoke the existing helper, not duplicate it. Duplication risks divergence in future maintenance. |
| Dual-layer state_dict sharing | `load_state_dict` MUST propagate frozen quantizer scales (per `register_buffer` registration in `FakeQuantize.__init__`). Mitigation: explicit `torch.equal` check on `quant_h_in.scale` / `quant_h_out.scale` AND at least one weight quantizer's scale between the two layers BEFORE the parity assertion. |
| Helper recipe ↔ D-41 contract | The wrapper must produce the actual D-41 frozen recipe (not just `mode='dynamic'` from `_make_layer`'s default). Override quantizer modes and run inline `cell.freeze_quantizers()`. |
| Phase 2 fp32 strict-tier sections ↔ Phase 4 section | Verifier asserts no Phase 2 deletions/modifications (D-52). |
| D-22 OOB regression integrity | Phase 4 must NOT modify `tests/test_butterfly_dispatch.py:164` — only reference it in docstrings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-15 | Tampering | `_make_butterfly_layer_quant_int8` accidentally duplicates `_make_layer` instead of wrapping it | mitigate | Acceptance: wrapper calls `_make_layer(H, use_triton, hidden_bits=8)` — verified by grep on the new function body |
| T-04-16 | Tampering | `load_state_dict` does NOT propagate frozen scales → silent test corruption | mitigate | Early in each test body: assert `torch.equal` on `quant_h_in.scale` / `quant_h_out.scale` AND one weight quantizer's scale between the two layers; catches mismatch before parity assertion |
| T-04-17 | Repudiation | `_assert_quant_parity` diverges from Plan 04-02 / 04-03 (D-43 violation) | mitigate | Acceptance: helper body byte-for-byte identical to Plan 04-02/04-03 |
| T-04-18 | Tampering | D-22 OOB regression file accidentally edited | mitigate | Acceptance: `git diff -- tests/test_butterfly_dispatch.py` empty |
| T-04-19 | Tampering | Locked files touched | mitigate | `git diff` over locked files empty (D-51) |
| T-04-20 | Spoofing | Parallel-agent commit collision (Wave 2 race) | mitigate | Per-agent `git worktree` isolation OR sequential within Wave 2; staged commits use exact file paths |
| T-04-36 | Tampering | Wrapper silently uses `_make_layer`'s default `mode='dynamic'` quantizers instead of overriding to the actual D-41 frozen recipe | mitigate | Acceptance: `grep "freeze_quantizers" tests/test_triton_butterfly_strict.py` returns ≥ 1; recipe explicitly switches weight + input_act modes to min_max and freezes via cell.freeze_quantizers() |

</threat_model>

<verification>
- One commit modifying ONLY `tests/test_triton_butterfly_strict.py`.
- Dual-layer pattern + state-sharing assertion (on hidden AND weight scales) in fwd and bwd bodies.
- `_assert_quant_parity` uniform across all four Phase 4 strict files.
- Helper produces actual D-41 recipe via inline `cell.freeze_quantizers()`, not the default `mode='dynamic'` from `_make_layer`.
- D-22 regression and Phase 2 fp32 sections untouched.
- D-50, D-51, D-52 hygiene gates green.
</verification>

<success_criteria>
- All must-have truths verified.
- Plan 04-04 commit modifies ONLY the butterfly strict file.
- Butterfly closes out QNT-02 (alongside diagonal + monarch from Plan 04-03) and contributes to QNT-03.
</success_criteria>

<output>
After completion:
- `tests/test_triton_butterfly_strict.py` extended.
- `.planning/phases/04-quant-on-bit-identity/04-04-SUMMARY.md` written by execute-plan.

Wave 2 complete. Plan 04-05 (audit kickoff + CUDA GPU run + SUMMARY) is next.
</output>
