---
phase: 04-quant-on-bit-identity
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - .planning/phases/04-quant-on-bit-identity/04-FINDINGS.md
  - .planning/phases/04-quant-on-bit-identity/04-SUMMARY.md
autonomous: false
requirements: [QNT-01, QNT-02, QNT-03, QNT-04]

must_haves:
  truths:
    - "User runs the full Phase 4 quant-on test suite on a CUDA box (`pytest tests/test_triton_*_strict.py -k 'quant' -v -m 'not slow'` + slow variant) and reports results via the `checkpoint:human-verify` resume signal."
    - "Every failure observed in the GPU run has a corresponding `bd create` issue per D-50 / D-37."
    - "Every finding follows the two-commit failing-test-before-fix discipline per D-37 / D-50 — Commit A is the existing failing test from Plans 04-02..04 (or a more-targeted regression test if the failure is shape-/case-specific); Commit B is the fix (test bound, src/, or kernel as appropriate)."
    - "No `@pytest.mark.xfail` introduced anywhere across Phase 4 commits (`grep -r xfail tests/test_triton_*_strict.py tests/test_quantizers.py` returns empty)."
    - "On phase exit: `pytest tests/test_triton_*_strict.py -k 'quant' -v` passes on CUDA at the D-42 disposition-resolved bound."
    - "`tests/test_quantizers.py` has the QNT-04 regression test (Plan 04-01) passing on CUDA AND CPU (it's a CPU-runnable test)."
    - "D-51 locked files unchanged across ALL Phase 4 commits: `git diff <phase-4-base>..HEAD -- tests/test_parity.py tests/test_layer_parity.py tests/test_structure.py` is EMPTY."
    - "D-52 Phase 2 fp32 strict-tier sections unchanged: `git diff <phase-4-base>..HEAD -- tests/test_triton_scan_strict.py tests/test_triton_diagonal_strict.py tests/test_triton_monarch_strict.py tests/test_triton_butterfly_strict.py | grep '^-' | grep -v '^---'` returns lines only from explicitly-Phase-4-discarded edits (if any from Plan 04-05 fixes); the Phase 2 line ranges are untouched."
    - "D-22 OOB regression (`tests/test_butterfly_dispatch.py:164`) still passes."
    - "`_assert_quant_parity` helper body is byte-for-byte identical across all four strict files (D-43 uniformity verified)."
    - "Phase-exit SUMMARY at `.planning/phases/04-quant-on-bit-identity/04-SUMMARY.md` exists, documents pass/fail per QNT-01..04, lists all bd issue IDs (the closed QNT-04 issue from Plan 04-01 + any new findings)."
  artifacts:
    - path: ".planning/phases/04-quant-on-bit-identity/04-FINDINGS.md"
      provides: "Per-finding record: which test failed, bd issue ID, root cause, Commit A and Commit B hashes, regression-test path."
      contains: "Markdown table with one row per finding; empty (with 'no findings' note) if GPU run is fully green."
    - path: ".planning/phases/04-quant-on-bit-identity/04-SUMMARY.md"
      provides: "Phase-4-exit document: goal achievement table, requirement coverage table (QNT-01..04), tolerance contract verification (D-42 disposition), findings summary, hygiene confirmations, hand-off to Phase 5."
      contains: "QNT-01|QNT-02|QNT-03|QNT-04|D-42|disposition"
  key_links:
    - from: ".planning/phases/04-quant-on-bit-identity/04-FINDINGS.md"
      to: "bd issues (external)"
      via: "bd issue ID per finding; Commit A hash + Commit B hash"
      pattern: "bd-[a-z0-9]+ — Commit A: <hash> — Commit B: <hash>"
    - from: ".planning/phases/04-quant-on-bit-identity/04-SUMMARY.md"
      to: ".planning/STATE.md (phase status flip) + .planning/ROADMAP.md (Phase 4 checkbox)"
      via: "Phase-exit signal — orchestrator marks Phase 4 complete after SUMMARY"
      pattern: "Phase 4: Quant-on bit-identity.*[x]"
---

<objective>
Phase-4-exit audit kickoff. Plans 04-01..04 authored against the planner's understanding (and Plan 04-01 validated the dense probe on CUDA at the checkpoint:human-verify), but the FULL grid of ~700+ Phase 4 quant-on test cases (dense + diagonal + monarch + butterfly × cls × shape × fwd/bwd) has not yet been validated on hardware. This plan drives the CUDA-box validation, triages findings, and writes the phase-exit SUMMARY.

Pattern mirrors Phase 2's `02-06-PLAN.md` and Phase 1's `01-05-PLAN.md`.

Three tasks:
1. **`checkpoint:human-verify`** — user runs the full Phase 4 quant-on test suite on a GPU machine + locked-file hygiene checks; pastes back the output.
2. **Findings triage** — Claude parses the GPU output, files `bd create` per distinct failure, drives Commit A → Commit B (test → fix) per D-50, populates `04-FINDINGS.md`.
3. **Phase-exit SUMMARY** — write `04-SUMMARY.md` covering all 4 QNT-* requirements, the resolved D-42 disposition, the closed QNT-04 issue from Plan 04-01, and any new findings. Update `STATE.md` / `ROADMAP.md` Phase 4 checkbox.

Purpose: QNT-01..04 phase verification, audit findings triage, fix-in-milestone per D-50, hand-off to Phase 5 (calibration + freeze lifecycle).

Output: `04-SUMMARY.md` + `04-FINDINGS.md` + (if any) bd issues + fix commits.

**This plan is NOT autonomous** — it pauses at the GPU checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-quant-on-bit-identity/04-CONTEXT.md
@.planning/phases/04-quant-on-bit-identity/04-PATTERNS.md
@.planning/phases/04-quant-on-bit-identity/04-DISPOSITION.md
@.planning/phases/04-quant-on-bit-identity/04-01-PLAN.md
@.planning/phases/04-quant-on-bit-identity/04-02-PLAN.md
@.planning/phases/04-quant-on-bit-identity/04-03-PLAN.md
@.planning/phases/04-quant-on-bit-identity/04-04-PLAN.md
@.planning/phases/02-triton-fast-path-parity-vs-reference/02-06-PLAN.md
@.planning/phases/02-triton-fast-path-parity-vs-reference/02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Test surfaces produced by Plans 04-01..04 -->

`tests/test_triton_scan_strict.py` (Plan 04-01 + 04-02):
- test_dense_quant_probe_bit_identity (probe; T=8, B=4, H=64 fixed)
- test_scan_quant_fwd[cls-T-B-H], test_scan_quant_bwd[cls-T-B-H] (parametrized)
- _slow variants of both

`tests/test_triton_diagonal_strict.py` (Plan 04-03 Task 1):
- test_diagonal_quant_fwd[cls-T-B-H], test_diagonal_quant_bwd[cls-T-B-H]
- _slow variants

`tests/test_triton_monarch_strict.py` (Plan 04-03 Task 2):
- test_monarch_quant_fwd[cls-T-B-H-nblocks], test_monarch_quant_bwd[cls-T-B-H-nblocks]
- _slow variants

`tests/test_triton_butterfly_strict.py` (Plan 04-04):
- test_butterfly_quant_fwd[cls-T-B-H], test_butterfly_quant_bwd[cls-T-B-H]
- _slow variants

`tests/test_quantizers.py` (Plan 04-01 QNT-04 Commit A + B):
- test_per_channel_min_max_observer_per_channel_running_stats (runs on CPU)

D-51 LOCKED files (must remain unchanged across Phase 4):
- tests/test_parity.py
- tests/test_layer_parity.py
- tests/test_structure.py

D-52 Phase 2 fp32 sections unchanged in:
- tests/test_triton_scan_strict.py (lines 1-446 area; everything BEFORE the Phase 4 section)
- tests/test_triton_diagonal_strict.py (everything BEFORE Phase 4 section)
- tests/test_triton_monarch_strict.py (everything BEFORE Phase 4 section)
- tests/test_triton_butterfly_strict.py (everything BEFORE Phase 4 section)

D-22 OOB regression (must still pass):
- tests/test_butterfly_dispatch.py::test_butterfly_triton_forward_scratch_oob_regression at line 164

`bd` workflow per CLAUDE.md: `bd create`, `bd update <id> --notes`, `bd close <id>`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: User runs Phase 4 quant-on test suite on a CUDA box</name>
  <what-built>
    Plans 04-01..04 have authored:
    - QNT-04 fix via two-commit failing-test-before-fix (`tests/test_quantizers.py` + `src/gru_qat/quantizers.py`); bd issue closed.
    - Plan 04-01 dense probe + Phase 4 section helpers + grid constants in `tests/test_triton_scan_strict.py`.
    - Plan 04-01 checkpoint:human-verify resolved D-42 disposition → `.planning/phases/04-quant-on-bit-identity/04-DISPOSITION.md`.
    - Plan 04-02 full dense quant-on sweep (fwd + bwd × cls × shape) in `tests/test_triton_scan_strict.py`.
    - Plan 04-03 diagonal + monarch quant-on sweeps in `tests/test_triton_diagonal_strict.py` + `tests/test_triton_monarch_strict.py`.
    - Plan 04-04 butterfly quant-on sweep in `tests/test_triton_butterfly_strict.py`.

    On CPU all tests collect and skip cleanly via `@cuda_only` / `pytest.importorskip("torch_structured")`. On CUDA they execute and must pass at the disposition-resolved bound from 04-DISPOSITION.md.
  </what-built>
  <how-to-verify>
    On a CUDA box (gru-triton repo, `uv` installed, CUDA + Triton + torch_structured present), run the commands below and paste the full output back into chat.

    **1. Phase 4 quant-on fast tier (all four kernels, fwd + bwd × 3 adversarial × shape grid — should complete in 10-30 min)**:
    ```bash
    cd /home/claroche/gru-triton
    uv run pytest \
        tests/test_triton_scan_strict.py \
        tests/test_triton_diagonal_strict.py \
        tests/test_triton_monarch_strict.py \
        tests/test_triton_butterfly_strict.py \
        -v -m "not slow" -k "quant" 2>&1 | tail -80
    ```
    Expected: all PASS at the bound resolved in 04-DISPOSITION.md (Result A: `torch.equal`; Result B/C: `abs_diff < h_scale`). Each failing test message includes the cls name, (T, B, H), max abs diff, and h_scale.

    **2. Phase 4 quant-on slow tier (T=512 cases — may take 30-60 min)**:
    ```bash
    uv run pytest \
        tests/test_triton_scan_strict.py \
        tests/test_triton_diagonal_strict.py \
        tests/test_triton_monarch_strict.py \
        tests/test_triton_butterfly_strict.py \
        -v -m slow -k "quant" 2>&1 | tail -50
    ```
    Expected: all PASS.

    **3. QNT-04 regression test (CPU-runnable but verify on the CUDA host for consistency)**:
    ```bash
    uv run pytest tests/test_quantizers.py -v 2>&1 | tail -15
    ```
    Expected: 9 active tests passed (including `test_per_channel_min_max_observer_per_channel_running_stats`).

    **4. D-22 OOB regression (must still pass per Phase 2)**:
    ```bash
    uv run pytest tests/test_butterfly_dispatch.py::test_butterfly_triton_forward_scratch_oob_regression -v 2>&1 | tail -10
    ```
    Expected: 1 passed.

    **5. Phase 2 fp32 strict-tier suite (sanity check — must NOT regress due to Phase 4 file extensions)**:
    ```bash
    uv run pytest \
        tests/test_triton_scan_strict.py \
        tests/test_triton_diagonal_strict.py \
        tests/test_triton_monarch_strict.py \
        tests/test_triton_butterfly_strict.py \
        -v -m "not slow" -k "not quant" 2>&1 | tail -30
    ```
    Expected: all PASS (Phase 2 strict tier from 02-SUMMARY).

    **6. D-51 locked files unchanged**:
    ```bash
    PHASE_BASE=$(git merge-base HEAD claude/unzip-and-prepare-repo-7JFrN 2>/dev/null || git rev-list --max-parents=0 HEAD | tail -1)
    git diff $PHASE_BASE..HEAD -- tests/test_parity.py tests/test_layer_parity.py tests/test_structure.py
    ```
    Expected: empty output.

    **7. D-50 no xfail across Phase 4 surface**:
    ```bash
    grep -rn "xfail" tests/test_triton_scan_strict.py tests/test_triton_diagonal_strict.py tests/test_triton_monarch_strict.py tests/test_triton_butterfly_strict.py tests/test_quantizers.py
    ```
    Expected: empty output.

    **8. D-43 uniformity — `_assert_quant_parity` body identical across the four strict files**:
    ```bash
    for f in tests/test_triton_scan_strict.py tests/test_triton_diagonal_strict.py tests/test_triton_monarch_strict.py tests/test_triton_butterfly_strict.py; do
        echo "=== $f ==="
        awk '/^def _assert_quant_parity/,/^def [^_]|^class /' "$f" | head -30
    done
    ```
    Expected: the four `_assert_quant_parity` blocks are byte-for-byte identical (modulo file boundaries).

    **9. D-52 Phase 2 fp32 sections unchanged**:
    ```bash
    git diff $PHASE_BASE..HEAD -- tests/test_triton_scan_strict.py tests/test_triton_diagonal_strict.py tests/test_triton_monarch_strict.py tests/test_triton_butterfly_strict.py | grep "^-" | grep -v "^---"
    ```
    Expected: empty output (only additions, no deletions to Phase 2 strict files).

    **10. mypy + ruff**:
    ```bash
    uv run mypy 2>&1 | tail -5
    uv run ruff check src tests 2>&1 | tail -5
    ```
    Expected: both green.

    Paste output for commands 1-10. If any fails, also paste the full pytest stack trace via `-v --tb=short` rerun.
  </how-to-verify>
  <resume-signal>
    Reply with the outputs of commands 1-10. Use:
    - `approved` — all green; proceed to Task 2 with no findings.
    - `findings: <description>` — one or more failures; Claude triages in Task 2.
    - `defer: <reason>` — no CUDA box accessible now; pause until later.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Findings triage — bd issues + Commit A → Commit B per finding</name>
  <files>.planning/phases/04-quant-on-bit-identity/04-FINDINGS.md</files>
  <read_first>
    - GPU run output from Task 1 (provided in user's resume signal)
    - .planning/phases/04-quant-on-bit-identity/04-CONTEXT.md (D-50 / D-51 / D-52 / D-43 / D-46)
    - .planning/phases/04-quant-on-bit-identity/04-DISPOSITION.md (disposition reminder)
    - .planning/phases/02-triton-fast-path-parity-vs-reference/02-06-PLAN.md (Phase 2 audit-kickoff template — Task 2 structure pattern)
    - .planning/phases/02-triton-fast-path-parity-vs-reference/02-FINDINGS.md (Phase 2 findings format reference)
    - CLAUDE.md (bd workflow)
  </read_first>
  <behavior>
    - If Task 1 resume signal is `approved`: create `04-FINDINGS.md` with a "No findings" note + the QNT-04 closure reference (from Plan 04-01) and proceed to Task 3.
    - If Task 1 resume signal is `findings: ...`: parse each failure, file `bd create` per distinct failure, drive Commit A → Commit B per D-50.
    - If Task 1 resume signal is `defer: ...`: halt; orchestrator pauses Phase 4 until CUDA is available.
  </behavior>
  <action>
    **Branch on the Task 1 resume signal.**

    **Path A: `approved` (no findings)**

    1. Create `.planning/phases/04-quant-on-bit-identity/04-FINDINGS.md`:
       ```
       # Phase 4 Findings

       **Phase:** 04-quant-on-bit-identity
       **GPU run completed:** <timestamp from Task 1>
       **D-42 disposition:** <Result A/B/C from 04-DISPOSITION.md>
       **Status:** No new findings.

       All Phase 4 quant-on tests passed at the disposition-resolved bound across:
       - tests/test_triton_scan_strict.py (dense)
       - tests/test_triton_diagonal_strict.py
       - tests/test_triton_monarch_strict.py
       - tests/test_triton_butterfly_strict.py

       Pre-existing finding closed during Phase 4:
       - QNT-04 (per-channel min_max observer): bd <id from Plan 04-01> — closed.
         Commit A: <hash> (failing test in tests/test_quantizers.py).
         Commit B: <hash> (fix in src/gru_qat/quantizers.py:_update_observer).

       Locked files (tests/test_parity.py, tests/test_layer_parity.py, tests/test_structure.py) unchanged.
       Phase 2 fp32 strict-tier sections unchanged.
       D-22 OOB regression still passes.
       No xfails introduced.
       _assert_quant_parity body uniform across all four strict files (D-43).
       ```
    2. Proceed to Task 3.

    **Path B: `findings: <details>` — one or more failures**

    For each distinct failure in the pytest output:

    1. **Identify the finding**: extract the test name (e.g., `test_diagonal_quant_bwd[near-saturation-64-32-512]`), parametrize id (`cls`, T, B, H, [nblocks]), the assertion message (max abs diff, gradient name if bwd, h_scale), and which kernel/file. **Bucket by failure shape**: many parametrize cases may share a root cause (e.g., all "near-saturation" cases for a given kernel) — file ONE bd issue per root cause, NOT per case.

    2. **Confirm two-commit eligibility per D-50**:
       - The failing test IS the Commit A artifact (already landed in Plans 04-02..04). Each strict-file extension contains the regression detector for the kernel + cls + shape that failed.
       - If the failure is from `test_dense_quant_probe_bit_identity` (the Plan 04-01 probe), it indicates the D-42 disposition itself is wrong — surface to the user and revise 04-DISPOSITION.md (re-open the Plan 04-01 checkpoint conceptually). This is a rare case and likely means Result A was selected but bit-identity fails outside the probe shape.

    3. **`bd create`**: file the issue with:
       - Title: `[Phase 4 / QNT-NN] <test_function_name> fails for <cls>, <T,B,H[,nblocks]> with max abs diff <value>` (or for buckets: `... fails across <N> cases — root cause: <hypothesis>`).
       - Body via `bd update <id> --notes`: pytest tail (`--tb=short`), file path of the failing test, root-cause hypothesis if obvious (e.g., "monarch near-saturation clipping mismatch — likely STE boundary flip differs between PyTorch ref and Triton kernel at scale_h * 127"), reference to the relevant src file (`src/gru_qat/triton_kernels/scan_*.py` or `src/gru_qat/quantizers.py` or `src/gru_qat/gru_cell.py`).
       - Capture the returned bd issue ID.

    4. **Investigate and fix (Commit B)**:
       - **If the fix is a test-bound change**: never silently loosen. Surface to user: "Test bound at <bound> is unachievable; observed max abs diff <value>. Options: (i) tighten kernel to meet the bound; (ii) accept the larger bound + log as known-but-accepted divergence; (iii) defer disposition revision to Phase 7." Wait for user signal. Only update the test after explicit user confirmation; add a docstring note + bd ID reference (NEVER an `@pytest.mark.xfail`).
       - **If the fix is a src/ change** (kernel bug, quantizer bug, gru_cell quant plumbing bug): write the fix. Modify ONLY the relevant `src/gru_qat/` file. Do NOT modify the failing test.
       - **If the fix touches D-51 locked files**: HALT. The locked files cannot be modified during Phase 4. Surface to user as a blocker.
    5. **Verify the fix on CUDA**: ask the user to re-run the specific failing test (or test bucket). Iterate fix → user-runs-test → commit until green.
    6. **`bd close <id>`** once the test passes. Append row to `04-FINDINGS.md`:
       ```
       | <test_name>[<parametrize-id>] | bd-<id> | <root cause> | Commit A: <existing-test-commit from Plans 04-02..04> | Commit B: <fix-commit-hash> | <reg-test-path> |
       ```
    7. Repeat per distinct finding (per root-cause bucket).

    Final `04-FINDINGS.md` structure (markdown table + leading summary paragraph + reference to closed QNT-04 from Plan 04-01).

    Rules:
    - **No `@pytest.mark.xfail`** anywhere (D-50). Failures are either fixed or surfaced as known-but-accepted via explicit user confirmation + docstring note + bd issue.
    - **No silent disposition loosening**. If a failure indicates the D-42 disposition needs revising, surface to user — do not unilaterally change the bound.
    - **Locked files MUST NOT be touched**: `tests/test_parity.py`, `tests/test_layer_parity.py`, `tests/test_structure.py`. Verifier asserts in Task 3.
    - **Phase 2 fp32 sections MUST NOT be touched** (D-52): any Phase 4 fix that lands in a strict file goes in the Phase 4 section ONLY.

    **Path C: `defer`** — halt; write a one-line note to STATE.md. Orchestrator resumes Phase 4 when CUDA is available.
  </action>
  <verify>
    <automated>cd /home/claroche/gru-triton && test -f .planning/phases/04-quant-on-bit-identity/04-FINDINGS.md && echo "FINDINGS exists"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/04-quant-on-bit-identity/04-FINDINGS.md` exists.
    - Path A: file says "No new findings" with timestamp and references the closed QNT-04 bd issue from Plan 04-01.
    - Path B: file contains a markdown table with one row per distinct finding (or per root-cause bucket), each with a `bd-` issue ID matched by `bd show <id>` returning a valid record.
    - For every Commit B in the table, `git log <hash>` shows the commit exists and modifies a file under `src/gru_qat/` OR (rarely, with explicit user approval) updates a Phase 4 test docstring with a known-but-accepted note + bd-ID reference (NEVER `@pytest.mark.xfail`).
    - For every Commit A reference, `git log <hash>` shows the commit exists in Plan 04-02..04 history.
    - `git log --oneline` for Phase 4 shows test-commits precede fix-commits chronologically per finding.
    - `grep -rn xfail tests/test_triton_*_strict.py tests/test_quantizers.py` returns nothing (D-50).
    - `git diff <phase-4-base>..HEAD -- tests/test_parity.py tests/test_layer_parity.py tests/test_structure.py` returns empty (D-51).
    - Path A: full Phase 4 quant-on suite green on CUDA per Task 1.
    - Path B: re-running the full quant-on suite after all Commit B fixes shows green.
  </acceptance_criteria>
  <done>
    All Phase 4 findings (if any) have a bd issue, Commit A test, Commit B fix, and a row in `04-FINDINGS.md`. Phase 4 quant-on suite fully green on CUDA. No xfails. Locked + Phase-2 files untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Phase-exit SUMMARY</name>
  <files>.planning/phases/04-quant-on-bit-identity/04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/04-quant-on-bit-identity/04-FINDINGS.md (from Task 2)
    - .planning/phases/04-quant-on-bit-identity/04-DISPOSITION.md (D-42 resolution)
    - .planning/phases/04-quant-on-bit-identity/04-01-SUMMARY.md (Plan 04-01 SUMMARY, includes QNT-04 closure)
    - .planning/phases/04-quant-on-bit-identity/04-02-SUMMARY.md (dense full sweep)
    - .planning/phases/04-quant-on-bit-identity/04-03-SUMMARY.md (diagonal + monarch)
    - .planning/phases/04-quant-on-bit-identity/04-04-SUMMARY.md (butterfly)
    - .planning/phases/02-triton-fast-path-parity-vs-reference/02-SUMMARY.md (template — match section structure)
    - .planning/REQUIREMENTS.md (QNT-01..04 entries)
    - $HOME/.claude/get-shit-done/templates/summary.md (standard template)
  </read_first>
  <action>
    Write `.planning/phases/04-quant-on-bit-identity/04-SUMMARY.md` following the Phase 2 template (02-SUMMARY.md). Required sections:

    1. **YAML frontmatter** with `phase: 04-quant-on-bit-identity`, `verified`, `status` (`passed` if Path A or all Path B fixes green; `partial` if any open findings deferred to Phase 5/7; `failed` if blockers remain), `score` (truths verified / total), `re_verification: false`, `requirements-completed: [QNT-01, QNT-02, QNT-03, QNT-04]`, `tech-stack.patterns` (e.g., "Frozen-INT8 short-circuit pattern; disposition-aware `_assert_quant_parity` helper uniform across four strict files (D-43)"), `key-files.modified` (the four strict files + tests/test_quantizers.py + src/gru_qat/quantizers.py), `key-files.created` (04-FINDINGS.md, 04-DISPOSITION.md, 04-SUMMARY.md), `affects` (Phase 5 inherits the validated quant-on surface; Phase 7 inherits any deferred-disposition bd issues).

    2. **Phase Goal** — restate ROADMAP Phase 4 goal verbatim.

    3. **D-42 Disposition** — quote the resolved disposition from 04-DISPOSITION.md. Explain the bound chosen (Result A / B / C) and the implication for the audit signal.

    4. **Goal Achievement Table** with the must-have truths from this plan + Plan 04-01's truths (probe + QNT-04 + disposition); Status (VERIFIED / NOT-VERIFIED) + Evidence (test names, commit hashes, grep outputs).

    5. **Requirement Coverage Table**:
       | REQ-ID | Statement | Test Function(s) | Status |
       | QNT-01 | Dense Triton fwd with frozen INT8 recipe is bit-identical to reference | test_scan_quant_fwd (+ _slow) + test_dense_quant_probe_bit_identity | SATISFIED at <disposition> |
       | QNT-02 | Same bit-identity for Diagonal / Monarch / Butterfly | test_{diagonal,monarch,butterfly}_quant_fwd (+ _slow) | SATISFIED at <disposition> |
       | QNT-03 | Quant-on backward gradients bit-identical between Triton and reference | test_*_quant_bwd across all four kernels (+ _slow) | SATISFIED at <disposition> |
       | QNT-04 | Per-channel min_max observer resolved | test_per_channel_min_max_observer_per_channel_running_stats + src/gru_qat/quantizers.py:_update_observer | SATISFIED (FIXED, bd <id> closed) |

    6. **Tolerance Contract Verification** — confirm the disposition-resolved bound across all four kernels; cite D-51 (locked files unchanged); cite D-52 (Phase 2 sections unchanged); cite D-43 (`_assert_quant_parity` body byte-uniform).

    7. **Findings** — link to `04-FINDINGS.md`; summarize counts. For each finding: bd ID, root cause, Commit A and Commit B hashes.

    8. **QNT-04 closure detail** — call out the QNT-04 / ACT-01 fix specifically (D-44 / D-45): two-commit failing-test-before-fix in Plan 04-01, the per-axis amin/amax fix in `src/gru_qat/quantizers.py:_update_observer`, the regression test in `tests/test_quantizers.py`, bd issue closed.

    9. **Phase 4 Hygiene** section — confirm:
       - `git diff <phase-4-base>..HEAD -- tests/test_parity.py tests/test_layer_parity.py tests/test_structure.py` is empty (D-51).
       - `git diff <phase-4-base>..HEAD -- tests/test_triton_*_strict.py | grep '^-' | grep -v '^---'` does NOT touch Phase 2 fp32 sections (D-52).
       - `grep -r xfail tests/test_triton_*_strict.py tests/test_quantizers.py` empty (D-50).
       - bd issue count matches finding count (plus the closed QNT-04 from Plan 04-01).
       - D-22 OOB regression still passes (referenced, untouched).

    10. **Next Phase Readiness — Phase 5** (Calibration + freeze lifecycle):
        - Phase 5 inherits the validated quant-on surface as the post-calibration round-trip target (CAL-03).
        - Phase 5 will exercise calibrate→freeze and assert post-freeze Triton matches reference at the same disposition-resolved bound from D-42.
        - No blockers to Phase 5 (or list any).

    11. **Per-Plan SUMMARY references**: 04-01-SUMMARY.md, 04-02-SUMMARY.md, 04-03-SUMMARY.md, 04-04-SUMMARY.md, 04-FINDINGS.md, 04-DISPOSITION.md.

    After writing:
    1. Update `.planning/STATE.md` — mark Phase 4 as completed in the progress table and `Current Position`.
    2. Update `.planning/ROADMAP.md` — flip the Phase 4 checkbox from `[ ]` to `[x]` with completion date.
    3. Commit:
       ```bash
       git add .planning/phases/04-quant-on-bit-identity/04-SUMMARY.md .planning/phases/04-quant-on-bit-identity/04-FINDINGS.md .planning/STATE.md .planning/ROADMAP.md
       git commit -m "docs(04): phase-exit SUMMARY + FINDINGS for quant-on bit-identity audit"
       ```
  </action>
  <verify>
    <automated>cd /home/claroche/gru-triton && test -f .planning/phases/04-quant-on-bit-identity/04-SUMMARY.md && grep -c "QNT-01\|QNT-02\|QNT-03\|QNT-04" .planning/phases/04-quant-on-bit-identity/04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/04-quant-on-bit-identity/04-SUMMARY.md` exists.
    - `grep -c "QNT-01\|QNT-02\|QNT-03\|QNT-04"` returns at least 4 (all four QNT requirements addressed).
    - SUMMARY frontmatter has `status: passed|partial|failed`.
    - SUMMARY references `04-FINDINGS.md`, `04-DISPOSITION.md`, and every per-plan SUMMARY (04-01..04-04).
    - Requirement Coverage table has 4 rows (QNT-01..04), each marked SATISFIED (with disposition annotation) or with blocker reference.
    - QNT-04 closure detail section explicitly cites Commit A + Commit B hashes and the closed bd issue ID.
    - `.planning/ROADMAP.md` has Phase 4 checkbox flipped to `[x]` with date.
    - `.planning/STATE.md` reflects Phase 4 completion.
    - Final commit lands: `git log --oneline -1 .planning/phases/04-quant-on-bit-identity/04-SUMMARY.md` shows a docs(04) commit.
  </acceptance_criteria>
  <done>
    Phase 4 SUMMARY written, FINDINGS populated, all 4 QNT-* requirements traced, document committed. STATE.md + ROADMAP.md updated.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user ↔ Claude during GPU run | User runs commands on a CUDA box and pastes outputs; Claude trusts the pasted text |
| Claude ↔ bd issue tracker | `bd create`/`update`/`close` is canonical (CLAUDE.md) |
| Commit A (test) ↔ Commit B (fix) ordering | `git log --reverse` shows test commit precedes fix commit per D-50 |
| D-51/D-52 locked-files gate | Verifier asserts `git diff` over the locked file list is empty across the entire phase |
| D-43 disposition uniformity | Verifier scans the four strict files for byte-identical `_assert_quant_parity` bodies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-21 | Spoofing | User pastes fabricated GPU output | accept | Inherent; verifier asserts the commit chain via `git log`, not user claims |
| T-04-22 | Tampering | Phase 4 fix silently modifies cell-parity or layer-parity files | mitigate | Task 2 + 3 acceptance: `git diff` over D-51 locked files empty |
| T-04-23 | Tampering | Phase 4 fix modifies Phase 2 fp32 strict-tier sections | mitigate | Task 2 + 3 acceptance: `git diff | grep "^-"` over the four strict files shows only Phase-4-area deletions (if any from explicit fixes); Phase 2 lines untouched |
| T-04-24 | Repudiation | A finding lands without a bd issue or without Commit A | mitigate | 04-FINDINGS.md cross-references both; acceptance asserts `bd show <id>` validity and `git log` chronology |
| T-04-25 | Information disclosure | bd issue body contains repo paths and stack traces | accept | bd is project-local; no PII; no secrets in stack traces |
| T-04-26 | Denial of service | Phase 4 GPU run takes hours | accept | Fast tier ~10-30 min; slow tier 30-60 min — within budget |
| T-04-27 | Elevation of privilege | Claude pushes to remote main without explicit user request | mitigate | Per CLAUDE.md: NEVER push to main without user request |
| T-04-28 | Tampering | A Phase 4 commit silently introduces `@pytest.mark.xfail` | mitigate | Task 1 step 7 + Task 2/3 acceptance: `grep -r xfail` empty (D-50) |
| T-04-29 | Repudiation | D-43 uniformity violated — `_assert_quant_parity` diverges between strict files | mitigate | Task 1 step 8: diff the four `_assert_quant_parity` blocks; must be byte-identical |

</threat_model>

<verification>
After all three tasks complete:
- `04-SUMMARY.md` exists with all must-have truths and all 4 QNT-* requirements addressed.
- `04-FINDINGS.md` exists (either "no new findings" or table with bd-id-traced rows).
- Path A: full Phase 4 quant-on suite + Phase 2 fp32 sections pass on CUDA.
- Path B: every finding has bd-id + Commit A + Commit B; full suite green after Commit Bs.
- `git log --oneline phase-4-base..HEAD` shows: per-plan commits → fix commits (if any) → docs(04) SUMMARY commit.
- `git diff phase-4-base..HEAD -- tests/test_parity.py tests/test_layer_parity.py tests/test_structure.py` empty (D-51).
- D-52: Phase 2 fp32 sections of the four strict files unchanged.
- D-50: `grep -r xfail tests/test_triton_*_strict.py tests/test_quantizers.py` empty.
- D-43: `_assert_quant_parity` body byte-identical across the four strict files.
- D-22: OOB regression at `tests/test_butterfly_dispatch.py:164` still passes.
- `bd ready` shows no open Phase 4 issues (all closed or explicitly accepted with user confirmation).
- ROADMAP.md and STATE.md updated to reflect Phase 4 complete.
</verification>

<success_criteria>
- All must-have truths in this plan + Plans 04-01..04 verified.
- All 4 QNT-* requirements marked SATISFIED (with disposition annotation) or with explicit blocker + bd issue.
- `04-SUMMARY.md` + `04-FINDINGS.md` + (if any) fix commits land.
- Cell-parity + layer-parity + structure-parity locked files unchanged across Phase 4.
- Phase 2 fp32 sections unchanged across Phase 4.
- D-22 OOB regression unchanged.
- No `@pytest.mark.xfail` introduced.
- ROADMAP Phase 4 entry checkbox flipped to `[x]` with date.
- STATE.md `Current Position` reflects Phase 4 complete.
</success_criteria>

<output>
After completion:
- `.planning/phases/04-quant-on-bit-identity/04-SUMMARY.md` — phase-exit artifact.
- `.planning/phases/04-quant-on-bit-identity/04-FINDINGS.md` — finding record (empty or populated).
- `.planning/STATE.md` updated with Phase 4 completion.
- `.planning/ROADMAP.md` updated with Phase 4 checkbox.

Orchestrator inspects 04-SUMMARY.md to mark Phase 4 complete and present readiness for Phase 5 (calibration + freeze lifecycle).
</output>
