---
phase: 04-quant-on-bit-identity
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - tests/test_triton_diagonal_strict.py
  - tests/test_triton_monarch_strict.py
autonomous: true
requirements: [QNT-02, QNT-03]

must_haves:
  truths:
    - "`tests/test_triton_diagonal_strict.py` is extended with a Phase 4 section: `_make_diagonal_layer_quant_int8` helper (ACTUAL D-41 recipe — frozen INT8 per-channel weight + per-tensor input_act + per-tensor hidden via inline calibrate-then-freeze), `test_diagonal_quant_fwd` + `_bwd` parametrized over `QUANT_FAST_GRID` (18 cases per D-49) × 3 adversarial classes per D-46 = 54 cases per direction; `_slow` siblings over `QUANT_SLOW_GRID` (9 × 3 = 27) marked `@pytest.mark.slow`."
    - "`tests/test_triton_monarch_strict.py` is extended with a Phase 4 section: `_make_monarch_layer_quant_int8(in_size, hid, nblocks, h_scale)` helper (same actual-D-41 recipe + StructureConfig(kind='monarch', nblocks=nblocks)), `test_monarch_quant_fwd` + `_bwd` parametrized over `QUANT_MONARCH_FAST_GRID` (cases filtered by `H % nblocks == 0`) × 3 adversarial classes; `_slow` siblings under `QUANT_MONARCH_SLOW_GRID`."
    - "Both files import or redefine the disposition-aware `_assert_quant_parity(ref, tri, *, name, cls, T, B, H, h_scale)` helper from Plan 04-02. Idiom matches the resolved D-42 disposition in `.planning/phases/04-quant-on-bit-identity/04-DISPOSITION.md`. Idiom is UNIFORM across diagonal, monarch (and dense + butterfly) per D-43."
    - "Both files include the ASCII-rule section header `# Phase 4: Quant-on bit-identity ... Tolerance: per D-42 disposition (resolved at Plan 04-01 checkpoint)` per D-48."
    - "Test bodies apply input quantization BEFORE `F.linear` (per D-41 recipe): `xq = layer.cell.quant_x(x); gi = F.linear(xq, Wi_cat, bi_cat)`. Mirror of the Plan 04-01 probe pattern."
    - "Diagonal direct-kernel-call pattern from `tests/test_triton_diagonal.py:124-156` is the analog for the new tests (h_in_q / h_out_q tuple plumbing into `gru_scan_diagonal_forward_triton` / `_pytorch`); the diagonal `gru_scan_diagonal_forward_*` functions return single tensors `out` of shape `[T, B, H]`, same convention as `gru_scan` — extract any needed `h_T` as `out[-1]`."
    - "Monarch tests retain the `nblocks ∈ {2, 4, 8}` parametrize axis (filtered by H-divisibility) per D-49."
    - "Diagonal H values are restricted to `{32, 128, 512}` per D-49 — NO `H ∈ {1, 2, 8}` (those are Phase 6 edge-case territory)."
    - "All new tests decorated with `@cuda_only`. `_slow` siblings with `@pytest.mark.slow`."
    - "Phase 2 fp32 strict-tier sections of BOTH files UNCHANGED (D-52). Plan 04-01 Phase 4 dense section in `tests/test_triton_scan_strict.py` unaffected."
    - "D-51 locked files unchanged: `git diff <phase-4-base>..HEAD -- tests/test_parity.py tests/test_layer_parity.py tests/test_structure.py` is EMPTY."
    - "No `@pytest.mark.xfail` introduced anywhere (D-50)."
  artifacts:
    - path: "tests/test_triton_diagonal_strict.py"
      provides: "Diagonal kernel quant-on parity (fwd + bwd) sweep against the disposition-resolved bound; reuses Plan 04-02's _assert_quant_parity helper definition (or a local copy with identical signature/behavior — discretion based on the executor's preferred refactor strategy). _make_diagonal_layer_quant_int8 produces a fully-frozen actual-D-41-recipe layer via cell.freeze_quantizers() inline."
      contains: "def _make_diagonal_layer_quant_int8|def test_diagonal_quant_fwd|def test_diagonal_quant_bwd|freeze_quantizers"
    - path: "tests/test_triton_monarch_strict.py"
      provides: "Monarch kernel quant-on parity sweep with nblocks axis; same disposition idiom; same actual-D-41 recipe."
      contains: "def _make_monarch_layer_quant_int8|def test_monarch_quant_fwd|def test_monarch_quant_bwd|QUANT_MONARCH_FAST_GRID|freeze_quantizers"
  key_links:
    - from: ".planning/phases/04-quant-on-bit-identity/04-DISPOSITION.md"
      to: "Diagonal + Monarch quant-on assertion idiom"
      via: "_assert_quant_parity helper signature uniform across all four Phase 4 strict files (D-43)"
      pattern: "_assert_quant_parity"
    - from: "tests/test_triton_diagonal.py:124-156 (Phase 2 realistic-tier QAT analog)"
      to: "tests/test_triton_diagonal_strict.py Phase 4 section bodies"
      via: "h_in_q / h_out_q tuple plumbing pattern, direct-kernel-call shape; Phase 4 differs by also applying layer.cell.quant_x(x) before F.linear (D-41 recipe input_act)"
      pattern: "gru_scan_diagonal_forward_triton.*h_in_quant"
    - from: "tests/test_triton_monarch.py:130-210 (Phase 2 realistic-tier QAT analog)"
      to: "tests/test_triton_monarch_strict.py Phase 4 section bodies"
      via: "h_in_q / h_out_q + Wh_struct 4-D tensor construction; Phase 4 differs by quant_x(x)-before-F.linear"
      pattern: "gru_scan_monarch_forward_triton.*h_in_quant"
---

<objective>
**SKETCH — assertion idiom is TBD per Plan 04-01 checkpoint.** Filled in by executor after Plan 04-01's `04-DISPOSITION.md` lands.

Extend `tests/test_triton_diagonal_strict.py` and `tests/test_triton_monarch_strict.py` with Phase 4 sections per D-47/D-48. Each section adds:
- `_make_<kind>_layer_quant_int8` helper — ACTUAL D-41 recipe (frozen INT8 per-channel weight + per-tensor input_act + per-tensor hidden, via inline `cell.freeze_quantizers()` after a one-forward calibration). Mirror Plan 04-01's helper body shape; only the `StructureConfig` differs.
- Full quant-on parity grid: fwd + bwd × 3 adversarial classes × shape grid.
- `_slow` siblings.

Covers QNT-02 (in part — diagonal + monarch quant-on bit-identity) and QNT-03 (bwd gradients) for these two kernels. Butterfly is Plan 04-04.

Purpose: extend the Phase 4 surface to two more kernels in parallel with Plan 04-02 (dense) and Plan 04-04 (butterfly). All four kernels run with the SAME disposition-resolved assertion idiom per D-43.

Output: 2 file extensions; ~108 new fast cases (54 per kernel × 2 directions effectively) and ~54 slow per kernel × the H-divisible monarch subset.

**Wave 2 parallelism:** This plan modifies `test_triton_diagonal_strict.py` + `test_triton_monarch_strict.py`. Plan 04-02 modifies ONLY `test_triton_scan_strict.py`. Plan 04-04 modifies ONLY `test_triton_butterfly_strict.py`. Zero file overlap → safe to run all three in parallel. Use per-agent `git worktree` isolation per Phase 2 02-SUMMARY retrospective recommendation, or serialize within Wave 2 if isolation is not available.

**This plan is autonomous** — no checkpoint. The Plan 04-05 CUDA GPU run is the validation gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-quant-on-bit-identity/04-CONTEXT.md
@.planning/phases/04-quant-on-bit-identity/04-PATTERNS.md
@.planning/phases/04-quant-on-bit-identity/04-01-PLAN.md
@.planning/phases/04-quant-on-bit-identity/04-02-PLAN.md
@.planning/phases/04-quant-on-bit-identity/04-DISPOSITION.md
@CLAUDE.md

<interfaces>
<!-- Diagonal kernel — primary analog tests/test_triton_diagonal.py:124-239 -->

From `src/gru_qat/triton_kernels/scan_diagonal.py`:
```
def gru_scan_diagonal_forward_triton(
    gi, h0, Wh_diag, bh_cat, *, h_in_quant=None, h_out_quant=None
) -> torch.Tensor: ...

def gru_scan_diagonal_forward_pytorch(
    gi, h0, Wh_diag, bh_cat, *, h_in_quant=None, h_out_quant=None
) -> torch.Tensor: ...

def gru_scan_diagonal_backward_triton(...): ...
def gru_scan_diagonal_backward_pytorch(...): ...
```

From `tests/test_triton_diagonal_strict.py` (existing Phase 2 strict file — must remain unchanged for fp32 sections):
```
def _make_diagonal_layer(in_size: int, hid: int) -> GRULayer: ...   # at ~line 77-90
# FAST_DIAG_GRID, SLOW_DIAG_GRID — Phase 2 grids; DO NOT reuse for Phase 4.
```

<!-- Monarch kernel — primary analog tests/test_triton_monarch.py:130-210 -->

From `src/gru_qat/triton_kernels/scan_monarch.py`:
```
def gru_scan_monarch_forward_triton(
    gi, h0, Wh_struct, bh_cat, *, h_in_quant=None, h_out_quant=None
) -> torch.Tensor: ...
    # Wh_struct: 4-D tensor (3, nblocks, block_size, block_size) for the 3 gates
```

From `tests/test_triton_monarch_strict.py` (existing Phase 2 strict file):
```
def _make_monarch_layer(in_size: int, hid: int, nblocks: int) -> GRULayer: ...   # at ~line 74-90
# FAST_MONARCH_GRID, SLOW_MONARCH_GRID — Phase 2; DO NOT reuse for Phase 4.
```

From `src/gru_qat/structure.py`:
```
StructureConfig(kind="diagonal" | "monarch" | "butterfly" | "circulant" | "ldr", nblocks=...)
```

From `src/gru_qat/gru_cell.py`:
```
GRUCellQuant.quant_x: FakeQuantize       # built from recipe.input_act
GRUCellQuant.freeze_quantizers() -> None # switches all observer-mode quantizers to frozen
```

<!-- Shared Phase 4 disposition + grid constants -->

From `.planning/phases/04-quant-on-bit-identity/04-DISPOSITION.md` (Plan 04-01 output):
- Result A: `torch.equal`
- Result B: `abs_diff < h_scale`
- Result C: Result B + docstring + bd issue

From `tests/test_triton_scan_strict.py` (Plan 04-02 — for the `_assert_quant_parity` helper analog):
```
def _assert_quant_parity(ref, tri, *, name, cls, T, B, H, h_scale) -> None: ...
    # Disposition-aware switch.
```
Each Phase 4 strict file gets its own LOCAL copy of `_assert_quant_parity` (or imports from a shared util — discretion; current discipline is "per-file extension, no new module" per D-47, so prefer local copies with identical body).

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagonal kernel — Phase 4 section extension</name>
  <files>tests/test_triton_diagonal_strict.py</files>
  <read_first>
    - .planning/phases/04-quant-on-bit-identity/04-DISPOSITION.md (MANDATORY — gates the assertion idiom)
    - tests/test_triton_diagonal_strict.py (full — Phase 2 fp32 file)
    - tests/test_triton_diagonal.py lines 124-239 (canonical QAT fwd + bwd analog with h_in_q / h_out_q plumbing)
    - tests/test_triton_scan_strict.py Plan 04-01 + 04-02 sections (for `_make_dense_layer_quant_int8` body shape — diagonal helper mirrors it with StructureConfig(kind="diagonal"); for `_assert_quant_parity` helper shape; for input-quant-before-F.linear pattern)
    - .planning/phases/04-quant-on-bit-identity/04-PATTERNS.md (full — diagonal section including the revised `_make_diagonal_layer_quant_int8` helper body)
    - .planning/phases/04-quant-on-bit-identity/04-CONTEXT.md (D-43 / D-46 / D-47 / D-48 / D-49 / D-52)
  </read_first>
  <behavior>
    - Read 04-DISPOSITION.md. Halt if missing or unresolved (findings/defer).
    - Append a new section at the END of `tests/test_triton_diagonal_strict.py` (after all existing Phase 2 content).
    - Section header: same ASCII rule pattern as Plan 04-01 (D-48).
    - Section contents (in order):
      1. Local copy of `_assert_quant_parity` helper (disposition-aware; idiom matches 04-DISPOSITION.md; body byte-for-byte identical to Plan 04-02's helper).
      2. `_make_diagonal_layer_quant_int8(in_size, hid, h_scale=0.02) -> GRULayer` helper per 04-PATTERNS.md. ACTUAL D-41 recipe:
         - `QuantRecipe(weight=bits=8/axis=0/mode='min_max', input_act=bits=8/mode='min_max', hidden=bits=8/mode='frozen')`.
         - `StructureConfig(kind="diagonal")` for the hidden side.
         - Manually set `quant_h_in.scale = quant_h_out.scale = torch.tensor(h_scale)`, `.zero_point = torch.tensor(0.0)`.
         - Inline calibration forward: `layer.eval(); with torch.no_grad(): layer(torch.randn(8, 4, in_size)*0.5, torch.randn(4, hid)*0.5)`.
         - `layer.cell.freeze_quantizers()`.
      3. `_adversarial_inputs` helper — LOCAL copy of Plan 04-01's helper (same `cls ∈ {realistic, near-saturation, large-magnitude}` switch).
      4. Module-scope constants `QUANT_FAST_GRID` (18 cases) / `QUANT_SLOW_GRID` (9 cases) — LOCAL copy; DO NOT reuse Phase 2's `FAST_DIAG_GRID` / `SLOW_DIAG_GRID` (D-49 explicit). NO `H ∈ {1, 2, 8}` (Phase 6 territory).
      5. `test_diagonal_quant_fwd[cls-T-B-H]`: parametrize over cls (outermost) × `QUANT_FAST_GRID`. Body follows `tests/test_triton_diagonal.py:124-156` shape:
         - Build layer via `_make_diagonal_layer_quant_int8`.
         - Build inputs via `_adversarial_inputs(cls, ...)`.
         - Apply input quantization BEFORE the linear: `xq = layer.cell.quant_x(x); gi = F.linear(xq, Wi_cat, bi_cat)` (D-41 recipe consistency).
         - Extract `Wh_diag` / `bh_cat` from layer state (mirror Phase 2 strict diagonal body shape).
         - Run BOTH `gru_scan_diagonal_forward_triton(gi, h0, Wh_diag, bh_cat, h_in_quant=..., h_out_quant=...)` AND `_pytorch(...)` for the reference comparator.
         - Call `_assert_quant_parity` on `out` (the kernel returns a single tensor).
      6. `test_diagonal_quant_bwd[cls-T-B-H]`: same parametrize. Build autograd-through-`gru_scan_diagonal_forward_*` for both paths, backward via `.float().pow(2).sum().backward()`, assert on each of `dgi`, `dh0`, `dWh_diag`, `dbh` independently via `_assert_quant_parity`.
      7. `_slow` siblings over `QUANT_SLOW_GRID` with `@pytest.mark.slow`.
    - All test functions decorated `@cuda_only`.
    - DO NOT modify ANY existing line above the new section. Phase 2 fp32 strict-tier diagonal content is locked (D-52).
  </behavior>
  <action>
    1. Read 04-DISPOSITION.md. If missing or `findings` / `defer`, surface to orchestrator and halt.
    2. Open `tests/test_triton_diagonal_strict.py`. Find the end of the file (after all Phase 2 tests).
    3. Append blank line + Phase 4 ASCII section header.
    4. Append the local `_assert_quant_parity` helper, matching Plan 04-02's idiom byte-for-byte. Include docstring citing the resolved disposition.
    5. Append `_make_diagonal_layer_quant_int8` per 04-PATTERNS.md (revised body — actual D-41 recipe + inline calibrate-then-freeze).
    6. Append `_adversarial_inputs` (local copy of Plan 04-01's version) — same body, same `cls` switch.
    7. Append `QUANT_FAST_GRID` / `QUANT_SLOW_GRID` constants per D-49. NO H ∈ {1, 2, 8}.
    8. Append `test_diagonal_quant_fwd` per behavior block. Body applies `layer.cell.quant_x(x)` before `F.linear`. Use the direct-kernel-call shape from `tests/test_triton_diagonal.py:124-156` (NOT the autograd wrapper) — call `gru_scan_diagonal_forward_triton` and `_pytorch` separately and assert.
    9. Append `test_diagonal_quant_bwd` per behavior block. Use autograd-wrapper pattern (call `.backward()` on `.pow(2).sum()` on both sides). Per-gradient `_assert_quant_parity` calls: `dgi`, `dh0`, `dWh_diag`, `dbh`. Each gradient name surfaces in the failure message.
    10. Append `_slow` siblings.
    11. Hygiene gates:
        - `grep -r "xfail" tests/test_triton_diagonal_strict.py` → empty.
        - `git diff HEAD -- tests/test_triton_diagonal_strict.py | grep "^-" | grep -v "^---"` → empty (no Phase 2 modifications).
        - `git diff HEAD -- tests/test_parity.py tests/test_layer_parity.py tests/test_structure.py` → empty.
        - `grep "quant_x\|freeze_quantizers" tests/test_triton_diagonal_strict.py | wc -l` → ≥ 3 (helper freeze step + at least 2 test bodies running input-quant before linear).
    12. Commit. Stage ONLY `tests/test_triton_diagonal_strict.py`. Commit message:
        ```
        test(04-03): diagonal kernel quant-on full sweep per D-49 + D-46

        Extends tests/test_triton_diagonal_strict.py with Phase 4 section:
        - _assert_quant_parity helper (disposition: <Result-X from 04-DISPOSITION.md>)
        - _make_diagonal_layer_quant_int8 (actual D-41 recipe: frozen INT8
          per-channel weight + per-tensor input_act + per-tensor hidden via
          inline cell.freeze_quantizers())
        - _adversarial_inputs + QUANT_FAST/SLOW_GRID
        - test_diagonal_quant_fwd over 3 adversarial × QUANT_FAST_GRID
          (applies quant_x(x) before F.linear)
        - test_diagonal_quant_bwd same; per-grad (dgi, dh0, dWh_diag, dbh)
          assertions
        - _slow siblings under QUANT_SLOW_GRID

        Disposition per D-42 resolved at Plan 04-01 checkpoint.
        Phase 2 fp32 section unchanged (D-52). Locked files unchanged (D-51).
        No xfails (D-50). H ∈ {1, 2, 8} excluded per D-49 (Phase 6 territory).

        Covers QNT-02 (diagonal fwd), QNT-03 (diagonal bwd).
        ```
  </action>
  <verify>
    <automated>cd /home/claroche/gru-triton && uv run pytest tests/test_triton_diagonal_strict.py --collect-only -q 2>&1 | grep -E "test_diagonal_quant_(fwd|bwd)" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_triton_diagonal_strict.py` contains 4 new test functions: `test_diagonal_quant_fwd`, `_bwd`, `_fwd_slow`, `_bwd_slow`.
    - `_assert_quant_parity` helper exists locally and matches Plan 04-02's idiom.
    - `_make_diagonal_layer_quant_int8` is the actual-D-41-recipe helper (uses `bits=8` for weight/input_act/hidden and `cell.freeze_quantizers()` after inline calibration) — verifiable via `grep "freeze_quantizers" tests/test_triton_diagonal_strict.py`.
    - `_adversarial_inputs`, `QUANT_FAST_GRID`, `QUANT_SLOW_GRID` defined inside the Phase 4 section.
    - Each new test body calls `layer.cell.quant_x(x)` before `F.linear(...)`. Verifiable via `grep "quant_x" tests/test_triton_diagonal_strict.py | wc -l` ≥ 2.
    - `pytest --collect-only` reports at least 162 new diagonal quant-on test items.
    - Phase 2 fp32 sections byte-for-byte unchanged.
    - `grep -r xfail` empty. `git diff` over locked files empty.
    - On CPU, all new tests SKIP via `@cuda_only`.
  </acceptance_criteria>
  <done>
    Diagonal kernel quant-on full grid in the strict file. CUDA validation in Plan 04-05.
  </done>
</task>

<task type="auto">
  <name>Task 2: Monarch kernel — Phase 4 section extension</name>
  <files>tests/test_triton_monarch_strict.py</files>
  <read_first>
    - .planning/phases/04-quant-on-bit-identity/04-DISPOSITION.md (MANDATORY)
    - tests/test_triton_monarch_strict.py (full — Phase 2 fp32 file)
    - tests/test_triton_monarch.py lines 130-210 (canonical QAT fwd + bwd analog with `h_in_q` / `h_out_q` and `Wh_struct` 4-D tensor)
    - tests/test_triton_diagonal_strict.py post-Task-1 (for the `_assert_quant_parity` helper shape — copy local — and the input-quant-before-F.linear pattern)
    - .planning/phases/04-quant-on-bit-identity/04-PATTERNS.md (full — monarch section including the revised helper body)
    - .planning/phases/04-quant-on-bit-identity/04-CONTEXT.md (D-43 / D-46 / D-49)
  </read_first>
  <behavior>
    - Read 04-DISPOSITION.md. Halt if missing or unresolved.
    - Append Phase 4 section to `tests/test_triton_monarch_strict.py`. Phase 2 fp32 content unchanged (D-52).
    - Section header per D-48.
    - Section contents:
      1. Local copy of `_assert_quant_parity` helper (body byte-for-byte identical to Plan 04-02 / Task 1 above).
      2. `_make_monarch_layer_quant_int8(in_size, hid, nblocks=4, h_scale=0.02) -> GRULayer` per 04-PATTERNS.md. Actual D-41 recipe + `StructureConfig(kind="monarch", nblocks=nblocks)` + inline calibrate-then-freeze. Same `bits=8` recipe shape; same `cell.freeze_quantizers()` after one-forward calibration.
      3. Local copy of `_adversarial_inputs`.
      4. `QUANT_MONARCH_FAST_GRID = [(T, B, H, nblocks) for T in (8, 64) for B in (1, 4, 32) for H in (32, 128, 512) for nblocks in (2, 4, 8) if H % nblocks == 0]` per D-49 + 04-PATTERNS.md.
      5. `QUANT_MONARCH_SLOW_GRID` over T=512 only.
      6. `test_monarch_quant_fwd[cls-T-B-H-nblocks]`: parametrize over cls × `QUANT_MONARCH_FAST_GRID`. Body per `tests/test_triton_monarch.py:148-210` shape: build layer, apply `xq = layer.cell.quant_x(x)` BEFORE `F.linear` (D-41 input_act), extract Wh_struct (4-D) and bh_cat, call `gru_scan_monarch_forward_triton` and `_pytorch` with `h_in_quant` / `h_out_quant`, assert via `_assert_quant_parity`.
      7. `test_monarch_quant_bwd[cls-T-B-H-nblocks]`: same parametrize. Per-grad assertions on `dgi`, `dh0`, `dWh_struct`, `dbh`.
      8. `_slow` siblings over `QUANT_MONARCH_SLOW_GRID`.
    - All `@cuda_only`. Slow `@pytest.mark.slow`.
    - Monarch test file ALSO requires `pytest.importorskip("torch_structured")` at module top — verify it's already there from Phase 2; if not, add.
  </behavior>
  <action>
    1. Read 04-DISPOSITION.md. Halt if missing.
    2. Open `tests/test_triton_monarch_strict.py`. Verify `pytest.importorskip("torch_structured")` is at module top. If missing (shouldn't be — Phase 2 added it), add it.
    3. Find the end of the file. Append the Phase 4 section per behavior block.
    4. Per-grad assertions in `test_monarch_quant_bwd` name the offending tensor: `dgi`, `dh0`, `dWh_struct`, `dbh`.
    5. Test bodies apply `layer.cell.quant_x(x)` before `F.linear` — same as diagonal task above.
    6. Hygiene gates: `grep -r xfail` empty; Phase 2 section unchanged; locked files empty diff; `grep "quant_x\|freeze_quantizers" tests/test_triton_monarch_strict.py | wc -l` ≥ 3.
    7. Commit. Stage ONLY `tests/test_triton_monarch_strict.py`. Commit message:
       ```
       test(04-03): monarch kernel quant-on full sweep with nblocks axis

       Extends tests/test_triton_monarch_strict.py with Phase 4 section:
       - _assert_quant_parity helper (disposition: <Result-X from 04-DISPOSITION.md>)
       - _make_monarch_layer_quant_int8(nblocks) — actual D-41 recipe via
         inline cell.freeze_quantizers() (depends on QNT-04 fix from Plan 04-01)
       - _adversarial_inputs
       - QUANT_MONARCH_FAST/SLOW_GRID with nblocks ∈ {2,4,8}, H % nblocks == 0
       - test_monarch_quant_fwd over cls × shape × nblocks; applies
         quant_x(x) before F.linear (D-41 input_act consistency)
       - test_monarch_quant_bwd same; per-grad (dgi, dh0, dWh_struct, dbh)
       - _slow siblings

       Phase 2 fp32 section unchanged (D-52). Locked files unchanged (D-51).
       No xfails (D-50). torch_structured import-skip preserved.

       Covers QNT-02 (monarch fwd), QNT-03 (monarch bwd).
       ```
  </action>
  <verify>
    <automated>cd /home/claroche/gru-triton && uv run pytest tests/test_triton_monarch_strict.py --collect-only -q 2>&1 | grep -E "test_monarch_quant_(fwd|bwd)" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_triton_monarch_strict.py` contains 4 new test functions: `test_monarch_quant_fwd`, `_bwd`, `_fwd_slow`, `_bwd_slow`.
    - `_make_monarch_layer_quant_int8` is the actual-D-41-recipe helper (uses `bits=8` for weight/input_act/hidden and `cell.freeze_quantizers()`). Verifiable via `grep "freeze_quantizers" tests/test_triton_monarch_strict.py`.
    - `QUANT_MONARCH_FAST_GRID` enforces `H % nblocks == 0` (filter present in list comprehension).
    - Parametrize axes are `cls` outermost (so test IDs read `[realistic-8-4-32-2]`).
    - `_assert_quant_parity` body is byte-for-byte identical to the diagonal file's helper from Task 1 (D-43 uniformity).
    - Test bodies apply `layer.cell.quant_x(x)` before `F.linear(...)`. `grep "quant_x"` returns ≥ 2 in the new section.
    - Phase 2 fp32 monarch sections byte-for-byte unchanged.
    - `grep -r xfail` empty; locked-file `git diff` empty.
    - On CPU, all new tests SKIP via `@cuda_only` (or skip via missing `torch_structured` on systems without it — both acceptable).
  </acceptance_criteria>
  <done>
    Monarch kernel quant-on full grid in the strict file. CUDA validation in Plan 04-05.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 04-DISPOSITION.md ↔ both file extensions | Both Task 1 and Task 2 MUST use the same disposition idiom (D-43 uniformity). If the executor accidentally diverges between files, the audit signal is corrupted. |
| Phase 2 strict-tier sections ↔ Phase 4 sections | Verifier asserts no deletions to Phase 2 content in either file (D-52). |
| Parallel execution within Wave 2 | Plans 04-02, 04-03, 04-04 run in parallel; each touches disjoint files. Per Phase 2 02-SUMMARY retrospective: use per-agent `git worktree` isolation, or serialize within the wave. Never use `git add .` / `git add -A`. |
| Helper recipe ↔ D-41 contract | The `_make_<kind>_layer_quant_int8` helpers MUST be the actual D-41 recipe (frozen INT8 per-channel weight + per-tensor activation), NOT the bits=32 Identity-weight shortcut from the realistic-tier analogs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Tampering | Plan 04-03 modifies Phase 2 fp32 sections of diagonal_strict or monarch_strict | mitigate | Acceptance criteria: `git diff` shows only additions; Phase 2 lines byte-for-byte unchanged |
| T-04-12 | Repudiation | `_assert_quant_parity` diverges between diagonal_strict and monarch_strict (D-43 violation) | mitigate | Task 2 acceptance: helper body byte-for-byte identical to Task 1's helper |
| T-04-13 | Tampering | Locked files touched | mitigate | `git diff` over test_parity.py / test_layer_parity.py / test_structure.py empty (D-51) |
| T-04-14 | Spoofing | Parallel-agent commit collision (Phase 2 retrospective race) | mitigate | Per-agent `git worktree` isolation OR sequential execution within Wave 2; staged commits use exact file paths (no `git add .`) |
| T-04-34 | Tampering | Helper silently falls back to bits=32 Identity weights (degraded recipe) | mitigate | Acceptance: `grep "freeze_quantizers" tests/test_triton_diagonal_strict.py tests/test_triton_monarch_strict.py` returns ≥ 2 hits; recipe uses `bits=8` for weight, input_act, hidden |
| T-04-35 | Tampering | Test body skips `quant_x(x)` before `F.linear` — silent recipe mismatch | mitigate | Acceptance: `grep "quant_x"` returns ≥ 2 in each file |

</threat_model>

<verification>
- Two file extensions land atomically (one commit per file).
- Both files have UNIFORM `_assert_quant_parity` body (D-43 / cross-file idiom check).
- Both helpers implement the actual D-41 recipe via inline `cell.freeze_quantizers()`.
- Test bodies apply input quantization before the linear projection.
- Phase 2 fp32 sections, locked files, xfail gates all green.
</verification>

<success_criteria>
- All must-have truths verified.
- Both new commits modify ONLY their target file.
- `_assert_quant_parity` idiom uniform across the two files and Plan 04-02's dense file.
- Helpers produce actual D-41 recipe layers, not bits=32 Identity-weight shortcuts.
</success_criteria>

<output>
After completion:
- `tests/test_triton_diagonal_strict.py` extended (Task 1 commit).
- `tests/test_triton_monarch_strict.py` extended (Task 2 commit).
- `.planning/phases/04-quant-on-bit-identity/04-03-SUMMARY.md` written by execute-plan.

Plan 04-04 (butterfly) ran/runs in parallel.
</output>
