---
phase: 07-audit-report-findings-handling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # ruff cleanup of test files is handled via [tool.ruff.lint.per-file-ignores]
  # in pyproject.toml (RESEARCH.md recommendation) — the 5 test files are NOT
  # edited, so they are deliberately absent from files_modified.
  - src/gru_qat/triton_kernels/scan.py
  - src/gru_qat/triton_kernels/scan_diagonal.py
  - src/gru_qat/triton_kernels/scan_monarch.py
  - src/gru_qat/triton_kernels/scan_butterfly.py
  - src/gru_qat/ste.py
  - src/gru_qat/structure.py
  - src/gru_qat/gru_layer.py
  - src/gru_qat/gru_cell.py
  - src/gru_qat/quantizers.py
  - tests/test_scan_wrapper_validation.py
  - pyproject.toml
autonomous: true
requirements: [RPT-01, RPT-02]
must_haves:
  truths:
    - "Every bare `assert` for shape/dtype/is_cuda validation in the ~10 assert-bearing gru_scan* callees (the *_persistent / *_triton / gru_scan_forward functions reached from the public .apply wrappers) is converted to `if ... raise ValueError/RuntimeError`, surviving python -O"
    - "A malformed-shape / non-float32 / non-CUDA input raises ValueError/RuntimeError (never AssertionError) through the full public gru_scan* call path"
    - "mypy exits 0 against src/gru_qat (was ~145 errors)"
    - "ruff check src tests exits 0 (was ~23 errors)"
    - "The 7rj failing-test commit (Commit A) precedes the 7rj fix commit (Commit B) in git log"
  decisions:
    - "D-02: gru-triton-7rj is a FIX-bucket issue fixed in-phase with the D-37/D-50 two-commit failing-test-before-fix discipline, no @pytest.mark.xfail."
    - "D-06: mypy reaches 0 errors and `ruff check src tests` reaches 0 errors via systematic fixes + a ruff per-file-ignores block; gru-triton-4m6 is closed."
  artifacts:
    - path: "tests/test_scan_wrapper_validation.py"
      provides: "Shape/dtype validation tests exercising all 4 public gru_scan* entry points end-to-end"
      contains: "pytest.raises"
    - path: "pyproject.toml"
      provides: "mypy third-party overrides + ruff per-file-ignores for the lint cleanup"
  key_links:
    - from: "src/gru_qat/triton_kernels/scan.py gru_scan_forward / gru_scan_forward_persistent (the assert-bearing callees)"
      to: "ValueError on bad shape, reachable from the public gru_scan / gru_scan_persistent wrapper"
      via: "if ... raise ValueError mirroring GRULayer.forward guard"
      pattern: "raise (ValueError|RuntimeError)"
---

<objective>
Close `gru-triton-7rj` (assert→ValueError hardening of the shape/dtype validation in the `gru_scan*` call paths) and `gru-triton-4m6` (the ~145-mypy / ~23-ruff lint debt) in a single plan, because all three work items (7rj, mypy, ruff) converge on the 4 `scan*.py` Triton-kernel files and must not be written concurrently (the `gru-triton-u00` parallel-write race lesson).

Purpose: The audit must not report itself as "not green" — mypy/ruff green and hardened internal-API validation are milestone-close hygiene (D-06). The 7rj fix is a genuine FIX-bucket bug (bare `assert` is stripped under `python -O`, turning a malformed shape into a kernel deadlock/OOB instead of a clear error).
Output: Hardened `gru_scan*` validation in the assert-bearing callees, a new `tests/test_scan_wrapper_validation.py`, mypy 0 / ruff 0, `pyproject.toml` config (mypy overrides + ruff per-file-ignores).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-audit-report-findings-handling/07-CONTEXT.md
@.planning/phases/07-audit-report-findings-handling/07-RESEARCH.md

<interfaces>
<!-- The 7rj fix mirrors the Phase 6 GRULayer.forward guard. Convention to copy: -->
From src/gru_qat/gru_layer.py (GRULayer.forward, EDG-04 guard, ~lines 169-176):
  `if T == 0: raise ValueError("...T...")` style — `if <bad condition>: raise ValueError("<field name> ... <constraint>")`.

<!-- IMPORTANT (BLOCKER fix): the public `gru_scan` / `gru_scan_persistent` /
     `gru_scan_diagonal` / `gru_scan_monarch` / `gru_scan_butterfly_triton`
     functions are thin, assert-FREE `.apply` wrappers — converting THOSE does
     nothing. The actual `assert is_cuda` / `assert dtype == float32` /
     `assert <tensor>.shape == ...` validations live in the CALLEES those
     wrappers dispatch into. The fix MUST be applied in the callees. -->

The assert-bearing callee functions to harden (verified by reading the 4 scan*.py files):

  src/gru_qat/triton_kernels/scan.py:
    - `gru_scan_forward_persistent`  — asserts at lines 230, 233-235  (`gi.is_cuda`, `h0.shape`, `Wh_cat.shape`, `bh_cat.shape`).  Reached from public `gru_scan_persistent` via `GRUScanPersistentFunction.apply`.
    - `gru_scan_backward_persistent` — asserts at lines 660-664       (`h0.shape`, `Wh_cat.shape`, `bh_cat.shape`, `out.shape`, `dout.shape`). Reached on the backward pass of `gru_scan_persistent`.
    - `gru_scan_forward`             — asserts at lines 1661-1667     (`gi.is_cuda`+all, `gi.dtype == float32`, `h0.shape`, `Wh_cat.shape`, `bh_cat.shape`). Reached from public `gru_scan` via `GRUScanFunction.apply`.
    - `gru_scan_backward_triton`     — asserts at lines 1349-1353     (`h0.shape`, `Wh_cat.shape`, `bh_cat.shape`, `out.shape`, `dout.shape`). Reached on the backward pass of `gru_scan`.

  src/gru_qat/triton_kernels/scan_diagonal.py:
    - `gru_scan_diagonal_forward_triton`  — asserts at lines 230, 233 (`gi.is_cuda and Wh_diag.is_cuda`, `Wh_diag.shape == (3, H)`). Reached from public `gru_scan_diagonal`.
    - `gru_scan_diagonal_backward_triton` — assert at line 491        (`Wh_diag.shape == (3, H)`). Backward pass of `gru_scan_diagonal`.

  src/gru_qat/triton_kernels/scan_monarch.py:
    - `gru_scan_monarch_forward_triton`   — asserts at lines 324, 328-329 (`gi.is_cuda and Wh_struct.is_cuda`, `n_gates == 3`, `out_blksz == in_blksz == H // nblocks`). Reached from public `gru_scan_monarch`.
    - `gru_scan_monarch_backward_triton`  — assert at line 772            (`n_gates == 3 and out_blksz == in_blksz == H // nblocks`). Backward pass of `gru_scan_monarch`.

  src/gru_qat/triton_kernels/scan_butterfly.py:
    - `gru_scan_butterfly_forward_triton`  — asserts at lines 386, 389, 395, 398-399 (`gi.is_cuda`, `h0.shape`, `n_gates == 3 and two1/two2 == 2`, `H_PAD >= H` + pow2, `1 << log_H_pad == H_PAD`). Reached from public `gru_scan_butterfly_triton`.
    - `gru_scan_butterfly_backward_triton` — asserts at lines 974, 976-977 (`n_gates == 3 and two1/two2 == 2`, `H_PAD >= H` + pow2, `1 << log_H_pad == H_PAD`). Backward pass.

  (Line numbers are current-as-of-read; the executor must re-confirm by reading
  each file before editing — see Task 1 <read_first>. Do NOT touch `assert`s
  inside `@triton.jit` kernel bodies or internal-dispatch-contract asserts —
  those are programmer-error invariants per the project error-handling convention.
  Asserts inside the `_pytorch` reference helpers — e.g. `gru_scan_diagonal_forward_pytorch:90`,
  `gru_scan_monarch_forward_pytorch:88-90` — are out of scope: they are not on
  the Triton public call path 7rj is about; leave them.)

mypy baseline (measured live 2026-05-15): 145 errors / 10 files. ~124 (86%) in the 4 scan*.py — 3 root patterns:
  untyped @triton.jit/@triton.autotune decorators, missing kernel-fn annotations, torch-structured Module/Tensor union narrowing.
ruff baseline: 23 errors — E402 (9, pytest.importorskip idiom), F841 (8, intentional kernel bias loads), F401 (4), E731 (2).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 7rj — shape/dtype validation hardening in the assert-bearing callees (two-commit)</name>
  <read_first>
    - .planning/phases/07-audit-report-findings-handling/07-CONTEXT.md (D-02 FIX bucket, D-37/D-50 two-commit discipline)
    - .planning/phases/07-audit-report-findings-handling/07-RESEARCH.md (triage row 2: 7rj; "Don't Hand-Roll" — mirror GRULayer.forward)
    - src/gru_qat/triton_kernels/scan.py — confirm assert sites in `gru_scan_forward_persistent` (~230-235), `gru_scan_backward_persistent` (~660-664), `gru_scan_backward_triton` (~1349-1353), `gru_scan_forward` (~1661-1667); confirm the public `gru_scan` (~1569) / `gru_scan_persistent` (~1624) are assert-free `.apply` wrappers
    - src/gru_qat/triton_kernels/scan_diagonal.py — confirm assert sites in `gru_scan_diagonal_forward_triton` (~230-233), `gru_scan_diagonal_backward_triton` (~491)
    - src/gru_qat/triton_kernels/scan_monarch.py — confirm assert sites in `gru_scan_monarch_forward_triton` (~324-329), `gru_scan_monarch_backward_triton` (~772)
    - src/gru_qat/triton_kernels/scan_butterfly.py — confirm assert sites in `gru_scan_butterfly_forward_triton` (~386-399), `gru_scan_butterfly_backward_triton` (~974-977)
    - src/gru_qat/gru_layer.py (the EDG-04 GRULayer.forward ValueError guard convention to mirror)
    - bd show gru-triton-7rj (root cause + the exact malformed-shape cases to test)
  </read_first>
  <behavior>
    - Test: `gru_scan` (the public wrapper) called with a non-CUDA tensor raises ValueError/RuntimeError naming the offending input — the guard fires in the `gru_scan_forward` callee, propagates through `GRUScanFunction.apply`, surfaces to the caller. Never an AssertionError, never a deadlock.
    - Test: `gru_scan` called with a non-float32 dtype raises ValueError naming dtype (guard in `gru_scan_forward`)
    - Test: each of the 4 PUBLIC entry points (`gru_scan`, `gru_scan_diagonal`, `gru_scan_monarch`, `gru_scan_butterfly_triton`) rejects a malformed shape with a clear ValueError naming the offending dimension — exercised end-to-end through the public call path, so the test proves the guard is reachable, not redundant
    - Test: `gru_scan_persistent` likewise rejects a malformed shape (its callee `gru_scan_forward_persistent` carries its own asserts)
    - Test: the validation still fires under `python -O` semantics (i.e. it is an `if ... raise`, not an `assert`) — verified by asserting the raised type is ValueError/RuntimeError, never AssertionError
  </behavior>
  <action>
    Commit A (failing test FIRST): create `tests/test_scan_wrapper_validation.py` with `pytest.raises(ValueError)` (or RuntimeError) cases that call the PUBLIC entry points — `gru_scan`, `gru_scan_persistent`, `gru_scan_diagonal`, `gru_scan_monarch`, `gru_scan_butterfly_triton` — with a non-CUDA tensor, a non-float32 dtype, and a malformed shape. Driving the public path (not the callees directly) is the point: it proves the converted guard is on the live call path. Gate the file with the existing `pytest.importorskip("triton")` + `cuda_only` idiom where a case needs CUDA to reach the guard; where the guard fires before any CUDA op (e.g. the non-CUDA-tensor case), keep that case CPU-runnable. Commit message: `test(07-01): add failing wrapper-validation tests for gru-triton-7rj`.
    Commit B (fix): in the CALLEE functions enumerated in `<interfaces>` — NOT the assert-free public `.apply` wrappers — convert every bare `assert` used for shape/dtype/`is_cuda` validation to `if <bad condition>: raise ValueError(...)` (or RuntimeError for state misuse), mirroring the `GRULayer.forward` EDG-04 guard convention — message names the offending field and constraint. The ~10 callee functions are: `gru_scan_forward`, `gru_scan_forward_persistent`, `gru_scan_backward_triton`, `gru_scan_backward_persistent` (scan.py); `gru_scan_diagonal_forward_triton`, `gru_scan_diagonal_backward_triton` (scan_diagonal.py); `gru_scan_monarch_forward_triton`, `gru_scan_monarch_backward_triton` (scan_monarch.py); `gru_scan_butterfly_forward_triton`, `gru_scan_butterfly_backward_triton` (scan_butterfly.py). Do NOT add a redundant guard to the assert-free public wrapper — the converted `if ... raise` must live in the callee that the wrapper dispatches into. Do NOT touch `assert`s inside `@triton.jit` kernel bodies, internal-dispatch-contract asserts, or the `_pytorch` reference helpers. Commit message: `fix(07-01): harden gru_scan shape/dtype validation in callees (gru-triton-7rj)`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_scan_wrapper_validation.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `uv run pytest tests/test_scan_wrapper_validation.py -q` exits 0
    - Behavior: each of the 4 public entry points rejects a malformed shape/dtype/non-CUDA input with ValueError/RuntimeError (never AssertionError) when called through its public name
    - Source: the converted `if ... raise` lives in the assert-bearing CALLEE reachable from the public call path — NOT as a redundant guard on the assert-free `.apply` wrapper. Verify: `grep -nE 'raise (ValueError|RuntimeError)' src/gru_qat/triton_kernels/scan*.py` shows the new guards inside the enumerated callee functions; `grep -cE '^ *assert ' src/gru_qat/triton_kernels/scan.py scan_diagonal.py scan_monarch.py scan_butterfly.py` for shape/dtype lines in those ~10 functions returns 0 (every such bare assert converted)
    - Source: `git log --oneline` shows the `test(07-01)` commit SHA precedes the `fix(07-01)` 7rj commit SHA
  </acceptance_criteria>
  <done>Every bare shape/dtype/is_cuda `assert` in the ~10 assert-bearing `gru_scan*` callees is converted to `if ... raise`, malformed input raises ValueError/RuntimeError through the full public call path, the new test file passes, and Commit A precedes Commit B in git log.</done>
</task>

<task type="auto">
  <name>Task 2: 4m6 — mypy 0 + ruff 0 lint cleanup</name>
  <read_first>
    - .planning/phases/07-audit-report-findings-handling/07-CONTEXT.md (D-06 — both src and tests green; do NOT weaken `strict = true`)
    - .planning/phases/07-audit-report-findings-handling/07-RESEARCH.md (the mypy/ruff cleanup section: per-code error tables, Pitfall 3 ordering, Don't-Hand-Roll overrides row — note the per-file-ignores recommendation for E402/F841)
    - src/gru_qat/triton_kernels/scan.py, scan_diagonal.py, scan_monarch.py, scan_butterfly.py, ste.py, structure.py, gru_layer.py, gru_cell.py (the 10-file mypy surface)
    - pyproject.toml ([tool.mypy], [tool.ruff] current config)
    - bd show gru-triton-4m6 (the tracked debt)
  </read_first>
  <action>
    Close `gru-triton-4m6`. mypy (strict, config-scoped to `src/gru_qat`): clear all ~145 errors. Per RESEARCH.md the bulk is 3 systematic patterns in the 4 `scan*.py` files — add `[[tool.mypy.overrides]] ignore_missing_imports = true` for the `triton` / `torch_structured` modules (their `import-untyped` errors), annotate or `# type: ignore[misc]` the `@triton.jit`/`@triton.autotune` decorated kernels, and narrow the `torch_structured` Module/Tensor unions in `scan_butterfly.py` with `assert isinstance(...)` / `cast()`. Apply per-line fixes for the ~30 `ste.py`/`structure.py`/`gru_layer.py`/`gru_cell.py` one-offs. Do NOT weaken `strict = true` — use scoped overrides for missing third-party stubs only.
    ruff (`ruff check src tests`): clear all ~23 errors. RUFF APPROACH (decided per RESEARCH.md, WARNING 4): use a `[tool.ruff.lint.per-file-ignores]` block in `pyproject.toml` for `E402` (the `pytest.importorskip` idiom in the 5 test files) and `F841` (intentional kernel bias loads in `scan_butterfly.py`) — these are deliberate patterns the project endorses, and per-file-ignores leaves the test files UNTOUCHED (which is why they are absent from this plan's `files_modified`). Delete the 4 genuine `F401` unused imports (in src files); convert the 2 `E731` lambda assignments to `def` (in src files). Do NOT scatter `# noqa` comments into the test files.
    PITFALL (RESEARCH.md Pitfall 3): fix each underlying typed error BEFORE removing the now-redundant `# type: ignore` (15 `unused-ignore` errors) — wrong order re-introduces real errors. Fix-and-delete the same line atomically.
    Behavior-regression gate (WARNING 2): the lint cleanup mutates 10 src files including the 4 hot-path `scan*.py` kernels and converts lambdas to `def`. Before committing, run `uv run pytest tests/test_triton_scan.py tests/test_parity.py -q -m "not divergence"` (the touched-file subset — extend to any other test file whose src target was edited) and confirm it stays green; on a CPU host the Triton cases skip, so additionally run `uv run pytest tests/test_parity.py -q` to cover the non-Triton edits.
    Commit when both lint gates AND the regression gate are green: `fix(07-01): clear mypy/ruff debt to 0/0 (gru-triton-4m6)`.
  </action>
  <verify>
    <automated>uv run mypy && uv run ruff check src tests && uv run pytest tests/test_triton_scan.py tests/test_parity.py -q -m "not divergence"</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `uv run mypy` exits 0
    - Behavior: `uv run ruff check src tests` exits 0
    - Behavior: `uv run pytest tests/test_triton_scan.py tests/test_parity.py -q -m "not divergence"` exits 0 after the lint pass — the cleanup introduced no behavior regression in the touched src files (on a CPU host the Triton cases skip; `test_parity.py` still exercises the non-Triton edits)
    - Source: `pyproject.toml` still contains `strict = true` under `[tool.mypy]` (not weakened)
    - Source: `pyproject.toml` contains `[tool.ruff.lint.per-file-ignores]` for E402/F841 and `[[tool.mypy.overrides]]` for triton/torch_structured
    - Source: the 5 ruff-affected test files (`test_triton_scan.py`, `test_structure.py`, `test_butterfly_dispatch.py`, `test_triton_monarch.py`, `test_parity.py`) are NOT in `git diff --name-only` for the ruff portion — they are handled by per-file-ignores, not edited
  </acceptance_criteria>
  <done>`uv run mypy` and `uv run ruff check src tests` both exit 0; the touched-file pytest regression subset stays green; `strict = true` preserved; the 5 test files untouched (per-file-ignores approach); gru-triton-4m6 ready to close in Wave 3.</done>
</task>

</tasks>

<threat_model>
Phase 7 hardens input validation (7rj assert→ValueError) and (in plan 07-02) fixes a silent quantizer-correctness bug (n20); no new attack surface — net risk reduction. The 7rj change strengthens an internal Python API against malformed shapes (defensive coding), not a security control. No authentication, network, or untrusted-input boundary is added or modified.
</threat_model>

<verification>
- `uv run pytest tests/test_scan_wrapper_validation.py -q` exits 0 (CUDA host for CUDA-gated cases; CPU-runnable cases pass everywhere).
- `uv run mypy` exits 0; `uv run ruff check src tests` exits 0.
- `uv run pytest tests/test_triton_scan.py tests/test_parity.py -q -m "not divergence"` exits 0 — no lint-pass regression.
- `git log --oneline` shows `test(07-01)` 7rj commit precedes `fix(07-01)` 7rj commit.
</verification>

<success_criteria>
- 7rj closed: every bare shape/dtype/is_cuda `assert` in the ~10 assert-bearing `gru_scan*` callees raises `ValueError`/`RuntimeError`; the `python -O` deadlock/OOB path is dead; failing test committed before fix.
- 4m6 closed: mypy 0, ruff 0, `strict = true` preserved; touched-file regression subset green.
- No concurrent same-file write risk: this is the only Wave-1 plan touching `scan*.py`.
</success_criteria>

<output>
After completion, create `.planning/phases/07-audit-report-findings-handling/07-01-SUMMARY.md`.
</output>
