---
phase: 07-audit-report-findings-handling
plan: 04
type: execute
wave: 4
depends_on: [07-01, 07-02, 07-03]
files_modified:
  - AUDIT-REPORT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [RPT-01, RPT-02, RPT-03]
must_haves:
  truths:
    - "AUDIT-REPORT.md exists at repo root with all 4 D-08 sections"
    - "Section (a): a table of all 28 v1 requirements with status PASS / FIX / ACCEPTED-DIVERGENCE"
    - "Section (b): a per-phase summary sourced from NN-SUMMARY/NN-VERIFICATION files, including the D-09 git-log test-before-fix audit result"
    - "Section (c): a residual-divergences section with the TF32 family as ONE consolidated entry with per-issue sub-bullets, plus a subsection plainly stating the criterion-#3 reinterpretation (green gate = pytest -q -m \"not divergence\")"
    - "Section (d): a pointer from each finding to the bd issue that resolved it"
    - "REQUIREMENTS.md traceability shows RPT-01/02/03 Complete"
  artifacts:
    - path: "AUDIT-REPORT.md"
      provides: "the milestone-closing audit report"
      contains: "ACCEPTED-DIVERGENCE"
      min_lines: 150
  key_links:
    - from: "AUDIT-REPORT.md section (d)"
      to: "the 14 bd issues"
      via: "one finding-to-bd-id pointer per finding"
      pattern: "gru-triton-"
---

<objective>
Write `AUDIT-REPORT.md` at the repo root — the milestone-closing deliverable (D-08, RPT-03). This is the final wave, alone, because the report records the FINAL post-fix state: n20 fixed, 7rj hardened, mypy/ruff green, the `divergence` marker in place, and all 14 bd issues closed. It depends on all three prior plans.

Purpose: ROADMAP criterion #2 — the audit closes with a written report summarizing what was checked, what passed, what was fixed, and the residual known-but-accepted divergences with rationale. The report is pure aggregation: it sources existing `NN-SUMMARY.md`/`NN-VERIFICATION.md` artifacts and the Wave-3 git-log audit — it does NOT re-derive phase narratives.
Output: `AUDIT-REPORT.md` (new, repo root) with the 4 D-08 sections; `REQUIREMENTS.md` traceability updated to mark RPT-01/02/03 Complete.

This plan does NOT need a GPU — it is report authoring from existing artifacts.

PRECONDITION (Wave-2 GPU gate): the report records the FINAL post-fix strict-test state. Do NOT author it until plan 07-02 Task 2's CUDA `-m "not divergence"` gates have PASSED (not skipped) on a GPU host and Wave 3 has closed the 14 bd issues. An AUDIT-REPORT written on an unverified Wave 2 would report a state that was never confirmed green.

OUT OF SCOPE: the `input_precision="ieee"` TF32-elimination kernel rewrite (D-03 — explicitly deferred to v2). The report DOCUMENTS the TF32 family as accepted; it does not fix it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-audit-report-findings-handling/07-CONTEXT.md
@.planning/phases/07-audit-report-findings-handling/07-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-quant-on-bit-identity/04-DISPOSITION.md

<interfaces>
<!-- D-08 — AUDIT-REPORT.md structure, 4 sections, each sourced from existing artifacts: -->
(a) 28-requirement status table — REF-01..05, TRI-01..06, STR-01..03, QNT-01..04, CAL-01..03,
    EDG-01..04, RPT-01..03; status in {PASS, FIX, ACCEPTED-DIVERGENCE}. Source: REQUIREMENTS.md v1 list.
    Requirements touched by a TF32 divergence (TRI-01/03/04, QNT-01/02/03) are PASS-with-divergence —
    cite the consolidated divergence entry.
(b) per-phase summary — source 01..06 NN-SUMMARY.md + NN-VERIFICATION.md (quote/condense, do NOT
    re-derive). Where SUMMARY and VERIFICATION disagree (Phase 6 c2a staleness), VERIFICATION is
    authoritative. Include the D-09 git-log test-before-fix audit result from 07-git-log-audit.txt.
(c) residual known-but-accepted divergences — ONE consolidated TF32 entry with per-issue sub-bullets
    (in0, q3k, lqk, 5rk, mjy, lht, e7t, fpl, 6dz). State the root cause once; note e7t is tl.sum-rooted
    not tl.dot-rooted (same phenomenon, different op). Plus a separate INDIVIDUAL entry for e0l (HW)
    and a process-note for u00. Plus a subsection plainly stating the criterion-#3 reinterpretation:
    the green gate is `pytest -q -m "not divergence"`, not literal `pytest -q` — this is itself an
    audit finding (D-05).
(d) finding-to-bd pointers — one pointer per finding row to its bd issue (the 14 IDs).

<!-- SIZE EXPECTATION (WARNING 6): the report is substantial — a 28-row requirement
     table + 6 per-phase summaries + a 9-sub-bullet consolidated divergence section +
     14 finding-to-bd pointers + the criterion-#3 subsection. The artifact gate is
     min_lines: 150; a report materially shorter than that has almost certainly
     dropped a section or compressed it past usefulness. -->

Source artifacts (confirmed to exist):
  .planning/phases/0{1..6}-*/0{1..6}-SUMMARY.md, .planning/phases/0{2,4,5,6}-*/*-VERIFICATION.md,
  .planning/phases/04-quant-on-bit-identity/04-DISPOSITION.md,
  .planning/phases/07-audit-report-findings-handling/07-git-log-audit.txt (from plan 07-03),
  .planning/debug/repro_monarch_rounding.py.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author AUDIT-REPORT.md (4 D-08 sections) at repo root</name>
  <read_first>
    - .planning/phases/07-audit-report-findings-handling/07-CONTEXT.md (D-08 structure; D-05 criterion-#3 operationalization must be stated plainly; D-09 git-log gap documentation)
    - .planning/phases/07-audit-report-findings-handling/07-RESEARCH.md (the AUDIT-REPORT.md section — per-section source-artifact map; Pitfall 5 — do not re-derive)
    - .planning/REQUIREMENTS.md (the 28 v1 requirement IDs for section (a))
    - .planning/phases/01-*/01-SUMMARY.md through 06-*/06-SUMMARY.md and the 02/04/05/06 VERIFICATION.md files (section (b) source — VERIFICATION authoritative on disagreement)
    - .planning/phases/04-quant-on-bit-identity/04-DISPOSITION.md (the per-cluster h_scale_mult table for section (c))
    - .planning/phases/07-audit-report-findings-handling/07-git-log-audit.txt (the Wave-3 test-before-fix ordering result for section (b))
    - bd show <id> for the 14 issues (section (d) finding-to-bd pointers; closed-state resolution notes from Wave 3)
  </read_first>
  <action>
    Create `AUDIT-REPORT.md` at the repo root with the 4 D-08 sections:
    (a) A table of all 28 v1 requirements (REF-01..05, TRI-01..06, STR-01..03, QNT-01..04, CAL-01..03, EDG-01..04, RPT-01..03) with status in {PASS, FIX, ACCEPTED-DIVERGENCE} sourced from REQUIREMENTS.md. Mark TRI-01/03/04 and QNT-01/02/03 PASS-with-divergence and cite the section-(c) consolidated entry.
    (b) A per-phase summary (Phases 1-6) — quote/condense each `NN-SUMMARY.md` and `NN-VERIFICATION.md`; do NOT re-derive (Pitfall 5). Where Phase 6 SUMMARY and VERIFICATION disagree on c2a, VERIFICATION is authoritative. Embed the D-09 git-log test-before-fix audit result (from `07-git-log-audit.txt`): state which Phase 1-6 findings have test-before-fix ordering and which have documented gaps (Phases 1-3 predate D-37/D-50) — gaps are documented, not history-rewritten.
    (c) A "residual known-but-accepted divergences" section: ONE consolidated TF32 `tl.dot`/`tl.sum` reduction-order entry stating the root cause once, why the `input_precision="ieee"` fix is out of scope (PROJECT.md Option C Key Decision), with per-issue sub-bullets for `in0`, `q3k`, `lqk`, `5rk`, `mjy`, `lht`, `e7t`, `fpl`, `6dz` — annotate that `e7t` is `tl.sum`-rooted not `tl.dot`-rooted (same phenomenon, different op). Add a separate INDIVIDUAL entry for `e0l` (RTX 2000 Ada hardware limit, v2 kernel-tiling redesign) and a process-note for `u00`. Add a clearly-titled subsection that PLAINLY states the criterion-#3 reinterpretation: the honest green gate is `pytest -q -m "not divergence"` and `pytest -m "slow and not divergence" -q`, not literal `pytest -q` — this reinterpretation is itself an audit finding (D-05), intentional and documented, not a silent loosening.
    (d) A finding-to-bd-issue pointer for each of the 14 findings (one pointer per row to its bd ID, all now closed per Wave 3).
    SIZE (WARNING 6): the report aggregates a 28-row table, 6 per-phase summaries, 9 divergence sub-bullets, 2 INDIVIDUAL entries, the criterion-#3 subsection, and 14 finding pointers — expect ~150+ lines. Do not over-compress; the artifact gate is `min_lines: 150`.
  </action>
  <verify>
    <automated>test -f AUDIT-REPORT.md && [ "$(wc -l < AUDIT-REPORT.md)" -ge 150 ] && grep -q 'ACCEPTED-DIVERGENCE' AUDIT-REPORT.md && grep -q 'not divergence' AUDIT-REPORT.md && for r in REF-01 TRI-06 STR-03 QNT-04 CAL-03 EDG-04 RPT-03; do grep -q "$r" AUDIT-REPORT.md || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - Artifact: `AUDIT-REPORT.md` exists at the repo root with all 4 sections (a)-(d) and is at least 150 lines (`wc -l`)
    - Source: `grep -c -E 'REF-0[1-5]|TRI-0[1-6]|STR-0[1-3]|QNT-0[1-4]|CAL-0[1-3]|EDG-0[1-4]|RPT-0[1-3]' AUDIT-REPORT.md` confirms all 28 requirement IDs appear in section (a)
    - Source: `AUDIT-REPORT.md` contains the consolidated TF32 entry with sub-bullets for all 9 ACCEPTED-DIVERGENCE bd IDs and a separate `e0l` / `u00` entry
    - Source: `AUDIT-REPORT.md` contains a subsection plainly stating the criterion-#3 green gate is `pytest -q -m "not divergence"`
    - Source: `AUDIT-REPORT.md` section (d) contains a `gru-triton-<id>` pointer for each of the 14 findings
    - Behavior: the report does NOT propose or include the out-of-scope `input_precision="ieee"` rewrite as Phase 7 work
  </acceptance_criteria>
  <done>`AUDIT-REPORT.md` exists at repo root (≥150 lines) with all 4 D-08 sections, the 28-requirement table, the consolidated TF32 divergence entry, the criterion-#3 reinterpretation subsection, and finding-to-bd pointers for all 14 issues.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md traceability for RPT-01/02/03</name>
  <read_first>
    - .planning/REQUIREMENTS.md (the v1 RPT-01/02/03 entries and the Traceability table)
    - .planning/phases/07-audit-report-findings-handling/07-CONTEXT.md (RPT-01/02/03 are the Phase 7 requirements)
    - AUDIT-REPORT.md (the just-authored report — RPT-03's deliverable)
  </read_first>
  <action>
    In `.planning/REQUIREMENTS.md`: mark RPT-01, RPT-02, RPT-03 as `[x]` complete in the v1 RPT section with a brief completion note (RPT-01: git-log test-before-fix audit done, gaps documented; RPT-02: all 14 bd issues closed with resolution notes; RPT-03: `AUDIT-REPORT.md` authored at repo root). Update the Traceability table rows for RPT-01/02/03 from `Pending` to `Complete`. Do NOT touch the 25 already-complete v1 rows. Confirm any v2 bd deferral recorded by plan 07-03 (e.g. the e0l kernel-tiling redesign) is still present in the v2 section.
  </action>
  <verify>
    <automated>grep -E '\[x\].*RPT-0[1-3]' .planning/REQUIREMENTS.md | wc -l | grep -q '^3$' && grep -cE 'RPT-0[1-3].*Complete' .planning/REQUIREMENTS.md | grep -q '^3$'</automated>
  </verify>
  <acceptance_criteria>
    - Source: `.planning/REQUIREMENTS.md` shows RPT-01, RPT-02, RPT-03 marked `[x]` in the v1 RPT section
    - Source: the Traceability table shows RPT-01/02/03 rows as `Complete`
    - Source: the 25 pre-existing complete v1 rows are unchanged
  </acceptance_criteria>
  <done>RPT-01/02/03 marked complete in REQUIREMENTS.md v1 list and traceability table; v2 deferrals preserved.</done>
</task>

</tasks>

<threat_model>
Phase 7 hardens input validation (7rj) and fixes a silent quantizer-correctness bug (n20); no new attack surface — net risk reduction. This plan is pure documentation authoring; no code, no security surface.
</threat_model>

<verification>
- `test -f AUDIT-REPORT.md` passes; the file is ≥150 lines and contains all 28 requirement IDs, all 4 D-08 sections, the consolidated TF32 entry, and the criterion-#3 subsection.
- `.planning/REQUIREMENTS.md` shows RPT-01/02/03 complete in both the v1 list and the traceability table.
- Final phase gate (run on a CUDA host before `/gsd-verify-work`): `uv run pytest -q -m "not divergence"` green, `uv run pytest -m "slow and not divergence" -q` green, `uv run mypy` 0, `uv run ruff check src tests` 0, `bd list --status=open` empty.
</verification>

<success_criteria>
- ROADMAP criterion #2 met: `AUDIT-REPORT.md` exists at repo root (≥150 lines) with all 4 sections.
- RPT-01/02/03 closed in REQUIREMENTS.md.
- The report documents (not fixes) the TF32 family and plainly states the criterion-#3 reinterpretation.
</success_criteria>

<output>
After completion, create `.planning/phases/07-audit-report-findings-handling/07-04-SUMMARY.md`.
</output>
